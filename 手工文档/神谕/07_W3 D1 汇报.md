# 07 — W3 D1 执行汇报：屏障探针对照表（按 06 结语"探针对照表出来那天，发我看一眼"）

> 2026-07-06。D1 当日完成：接线 + 探针 + S2 + HumDial 门。全部产物可逐位复核
> （`docs/w3_d1_memo.md` 为完整记录；规格 `docs/engine_b_spec.md`；语义单源 `src/tact_core.py`）。

## 一、屏障探针对照表（core，nominal 1.0s，W2 缓存 80/80 命中，零 GPU，官方 exact）

| 夹 | δ | barrier on | barrier off |
|---|---|---|---|
| ecommerce_19 | 1.0 | **P** | **P**（见修正） |
| housing_25 | 1.0 | **P** | F |
| finance_12b | 1.5 | **P** | F |
| travel_10 | 1.5 | F | F |
| **rollback-17 @1.5** | | **12/17** | **10/17** |

**对 06 §一预注册（off=9/17、三夹全挂）的跑前修正，已实测兑现**：eco19 的救援 patch 是值中性 diff
（首个修订被 hold 合并进 launch，patch 打在已为 tablet 的 op 上），其 δ 翻转是**窗口保持 pending 的
快照效应**（到期 19.91 > EoU 19.55，对任意 δ>0.64 成立；pending 快照诱导补发 add_to_cart），与
commit/patch 先后无关。**屏障因果权重 = {hou25 (patch 3500), fin12b (patch 150)}，消融差 12 vs 10**。
修正在运行前登记（预注册纪律，探针 docstring / 台账 §4A 勘误 / 规格 §4 三处留痕）。
机制分层由此三级：**hold 合并（8 夹）→ 快照效应（eco19）→ 屏障（hou25、fin12b）**——图 2 骨架
（教义三）从"三层保护栈"精化为可逐夹指认的分层。

配套硬证据：**parity 21/21**——barrier-on 与冻结 grid v1 在全部确定性字段逐位相等（工具调用
函数/参数/名义时间戳、结构 latency、ack 路首响）；墙钟分量按"v1 档案 tool_wall 系 --workers 全局
RNG 打乱产物"信息化（10 条，非门控）。**双戳**：deferrals rescued 6 / committed 2，max_deferred
0.14s（=hou25 pets 的 21.542−21.402，与台账算术吻合）；off 臂恒 0。**grid v1 零重跑存活。**

## 二、门与回归

- 单测 37/37（纯 CPU：预算定律/屏障三释放路径/双戳/消融/引擎回路冒烟）；S2 旧单测过
  （deterministic_replay 仅速度断言在无 GPU 容器挂，1.8×<10×，判环境性，算力日复验）。
- **HumDial 100（phase a）**：judge 三判 60.75/62.00/67.00 → 63.25±3.13，对 64.0 Δ=−0.75，
  门 Δ≥−1 过；llm_timeout 0；`tact_*` 事件 **0**（无 phase 泄漏）；TTS 轮数逐夹一致 95/100。
  注记：本次三判全宽 6.25 > 惯常 ±2 带（rejection 类标签主导），且 serving 栈非 6/23 provenance
  （85GB 临时卡+临时配置）；按三判均值纪律补一个**对称基线**（W2 存档输出同样重判 3 次）后归档。

## 三、full 仪器读数（帧→TactEngine，流式 VAD；无硬预期，13 处偏差全数归因）

- **ε 带发现（最重要）**：流式 end 检测迟滞 ε（实测 0.03–0.13s，逐边界浮动）在 **[hold, hold+ε)
  带内决定 EoU 的存在性**——eco19（间隙 0.708）、hou25（0.676）、hou17b（0.67）三夹的多 EoU 结构
  在 live 感知下坍缩为单发，窗口的救援机会集合随之改变，与 δ 取值无关。规格 §1.5 的"阈值 ≈ Σ静默+ε"
  须加注这一"存在性效应"；它是 H6 残差的第一构成项，D2 首查。
- 单发决策的两个行为样本：hou25 全文含 "3500" 更正、单发仍写 3000（增量+patch 在 core 答对——
  修订埋在长语句时单发误读，利好叙事）；fin23 在 ε 平移的前缀上翻转为真的调用 modify_autopay
  （narrate-no-op 回避是刀口行为，prompt 五靶#4 的直接证据）。
- 混杂如实记录：本轮 serving 栈临时（84g/8192 配置），ε 结构类归因与模型无关不受影响；
  fin23/hou17b 的决策翻转不排除 server 数值面贡献，下个 GPU 日用 5 条缓存键绕缓存重调即可定界。

## 四、下一步（照 06 执行序，无需新裁断）

D2：live 全量重测（标准栈、A 档）+ 首响 1.141 构成分解（回填裁断 B）；ε 带发现并入残差排查首位。
零 GPU 并行批同步开工：oracle 前沿三数（暴露集已定界）、hou11 契约审计与多轮普查、λ(t) v2、
RB 增补两类目。判读请求仅一项：**ε 存在性效应是否需要作为 δ 准则的正式条款补进规格 §1.5**
（我们倾向补一句"live 轨的可救集合 = 间隙 > hold+ε 的修订"，其余照旧）。
