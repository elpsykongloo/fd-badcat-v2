# AGENTS.md — fd-badcat 持久记忆（所有代理必读）

> 单一真相源。CLAUDE.md 指向本文件。有重大事实变更时**更新本文件**，不要另开新文档。
> 最后更新：2026-07-18（后续增量条目为准；外部评审全条核验成立 → RB v2.3.0 已实现〔评审 §五 items 1–5 + 归属门，用户裁定范围〕）
> **增量更新：2026-07-17（覆盖上一行的“下一步”）**：RB v2.3 冻结内容库、Qwen TTS 构建、dev oracle 有效性门与 attr 第一轮均已机器收官；正式 config = `e1a515c29b8a`，完整收据 `exp/rb/build_v23/rb_dev23_receipt.json`。attr-v1 A 臂净 −2、B 臂净 +2、合并 0，且 A-L4 护栏损失，故首轮 FAIL；未改词条、未跑第二轮、未碰 test。
> **增量更新 2：2026-07-17（判读层；覆盖上一行的“下一步”）**：**attr 门封盘于第一轮（§16.6，剩余额度裁定不动用）**——结构性死因三联〔靶层惰性 L12 骨架 0/3 变、靶维反作用 A_L4_0055 在规则下反而产生规则禁止的错误绑定（baseline 全链 launch+正确 patch op3 / attr 增量 launch+错误 patch op2=search）、弥散副作用 ≥27/54 骨架被改而仅得非归属性偶然增益〕；WHICH 轴正确机制类记录为引擎级 patch 模式校验 + 结构化绑定槽（不建造），L12 保留为测量层。**scorer 冻结 v4 已立**（`exp/rb/scorer_freeze.json`：v2.3 七项勘误链 + dev23 oracle 门收据 + 新三文件哈希；v2.2.1 归档仍由收据内 v3 哈希背书）。**v2.3 test-911 单发协议已冻结 = `rb_test_protocol.md` §九**（15 providers：A 546 主臂×2+屏障/DAG/δ 网格/FC 三档〔臂 A 限定，臂 B tts 锚考题冲突〕/oracle + B 365 三臂；判读 R1–R8：屏障/三定律复现 + **L7 补偿经济学〔H-COMP1 = blocking≥TACT 的诚实逆风预注册；H-COMP2 = reverse 调用发生率，v2.2.1 恒 0〕** + L11 双工描述 + **L12 基线误绑率 n=36** + armB L8 复现 + L10 双率 + FC talk-over 前沿预注册化；armb_timing overlaps=0 为硬门；H-B1 不再重冻）。**下一步 = 用户按 §九单发 test-911；判读写 §十 → 文稿装配线。**
> **增量更新 3：2026-07-17（用户裁定"方法必须应对 L12"→ admission v1 已交付）**：全档案法医普查（归档 patch 自带 diff）把 misbinding 人口切成两半——**非法字段形态 9.7%**（v2.2.1 test 496/5,097 patch；主臂 25 夹含之、0/25 通过；层分布 L4 9/L10 5/L3 5/L7 2/L8 2/L6 2）+ **合法字段错语义形态**（六个反窗口夹中 4/6 = `{destination: Thailand}`，机械门拦不住）。**机制 = `src/admission.py` patch 模式门**（拒绝非法字段、全非法丢弃整 patch；**可证明无害**：gold 参数恰为声明参数集 ⇒ 非法键必然 canonical 失配，剥离只减不增距离；v1 不重定向——patch 值可能是旧值，转投会污染正确 op，重定向只记审计）；runner `--admission schema` 默认 off 逐字节不变；selftest **34→39**（含 junk-arg-only 失败翻转 off-False→on-True、合法 patch 逐字节不动、默认路径双跑逐位）。**§9.4 增补两 provider**（`rbt23_tact_d150_adm`/`rbt23_b_tact_d150_adm`，总 15→17，仍在观测前）+ 判读 R-ADM1（admission-only-loss=0 硬判据）/R-ADM2（增益诚实低预期 0–5 夹，=0 也入册）/R-ADM3（9.7% 非法率复现）。attr 词条门维持 §16.6 封盘；论文表述 = misbinding 机械半边由准入控制关闭、语义半边由 L12 定量刻画（收据 §16.7）。**下一步 = 用户按 §九（含 9.4）单发 test-911 共 17 providers；判读写 §十。**
> **增量更新 4：2026-07-17（test-911 判定收口 = `rb_test_protocol.md` §十；覆盖上一行"下一步"）**：17 providers 单发/0 重跑/复放逐字节，镜像容器全量独立重算 0 失配。**主表**：A TACT/blocking = .1740/.1832（配对 17/22 p=.52 = 统计平手）、B = .1425/.0959（19/2 p=2.2e-4）、oracle A/B = .9121/.8740；共同失败 429 夹 89% oracle 可过（扰动族抬了内容难度，机制侧天花板反升）。**裁决**：R1 屏障**历来最强**（29/0 p=3.7e-9；nobar .1209<blocking=净负三连）；R2 三定律复现（δ>1.5 增量全落 L5、**L7 全 δ 恒 0=只向补偿低头**）；R3 **H-COMP1 兑现**（L7-A blocking 14/44 vs TACT 0/44）+ **H-COMP2=0**（四 LLM 主臂 reverse 调用零次；oracle 44/44+计费 10/44 = 通路健康）⇒ "补偿是能力缺口非机制缺口"成立；**R4 L11 = 本批最强正结果**（TACT 15/27 vs blocking 0/27，p=6.1e-5——打断 agent 播报改口是窗口机制独占领地）；R5 L12 定量（6/33 vs 3/33；27 失败夹 4 含解析层错绑证据）；**R6 armB L8 未复现**（3/88 vs 1/88 n.s.——v2.2.1 的 34/0 部分归因坏时间轴回声件，诚实降级，反应式主战场迁至 L11）；R8 talk-over base/v1/silent = 25.3/16.2/1.9%；**R-ADM1 臂 A FAIL(1/10)→尸检=实现层 bug**：门把快照局部 id 直查全局 pending（A_L12_0001 类铁证：模型局部 id 绑定本来正确、引擎 resolve_ref 一直在静默纠正、门杀掉正确 patch），admission v1 臂作废退役 + §16.7 "可证明无害" 公开勘误（定理在解析后层成立、实现用在了解析前层）；R-ADM3 复现 9.9%。**臂 A "反转"正读**：L7 贡献 −14、其余层 +11——窗口保住全部旧阵地，新账单只有"模型不会调 reverse"一张；进论文三句话已写 §10.3。**下一步 = 文稿装配线（RB 章数字全齐）；金子集录制 + 四基准全文核对并行；admission v1.1/SV 门为可选新系统版本单发，不阻塞。**
> **增量更新 5：2026-07-17（用户裁定做 v1.1 → 已交付，待 dev 冒烟 + 单发）**：`src/admission.py::admit_decision_ops_v11` + runner `--admission schema11`（`schema` 保留仅供已退役 adm 臂归档复放）。v1.1 = 同一拒绝规则搬到**解析后层**：用引擎自己的 `resolve_ref`（含 `_localmap` 局部→真实 id 翻译）先定目标、逐字节镜像引擎的嵌套 `{"args":{...}}` 解包、解析失败/stale 一律原样放行；**不再做定理级无害主张**（剥离仅限"引擎将以垃圾参数应用到已解析目标"的键；二阶轨迹效应交经验门：dev 零损失在前、test R-ADM1' 零损失硬门在后）。selftest **39→45**，`adm11_localmap_correct_patch_untouched` 显式复刻杀死 v1 的 id 碰撞几何（真实 2/3 × 局部 1/2）并断言 v1 误拒/v1.1 放行的对照；规格与判读冻结 = `rb_design.md` §16.8 + `rb_test_protocol.md` §10.5（dev 冒烟两 provider〔零损失门〕→ test 单发两 provider `rbt23_*_adm11`〔种自 v2.3 主臂缓存〕；R-ADM1' 零损失硬门 / R-ADM2' 增益 0–5 夹诚实低标 / R-ADM3' 三审计数含 **v1 假阳性对照数**）。**下一步 = 用户跑 §10.5：dev 冒烟 → 过门则 test 单发两臂；判读并入 §十尾注。文稿装配线不被阻塞。**
> **增量更新 6：2026-07-17（admission v1.1 dev 门 + test 单发已收官；覆盖上一行“下一步”）**：来源提交 `1302ab54`，runner selftest **45/45**、freeze v4 逐哈希通过。dev A/B baseline→adm11 = **7/54→7/54、5/35→5/35**，两臂 gain/loss 均 0，故零损失门 PASS 后才进入 test。test 两 provider 各 live 一次、0 重跑：A **95/546=.1740**、B **52/365=.1425**，exact/state/U 与主臂逐位同，配对两臂均 adm11-only/main-only = **0/0**，故 **R-ADM1' 双 PASS**；R-ADM2' 增益 0/0 如低标预期。R-ADM3'：解析后拒绝 A **27/273=9.9%**、B **14/174=8.0%**，`wire_unwrapped` 11/7；v1 假阳性对照 A **52 patches/51 夹**、B **24/22**（全为 raw local id≠resolved id，nested-wire 10/6），量化 v1 错杀面。首跑 cache A/B = 1431/1、951/3，四个 HTTP miss 全 200；TTS cache dev/test 73/725 unique 0 missing、正式 0 合成、armB overlaps=0。停服/GPU 清零后 test A/B 复放 **1432/0、954/0**，report/cache/911 逐夹三类逐字节同；反事实旁路复放亦不改字节。完整判定 `rb_test_protocol.md` §10.6，机器收据 `exp/rb/build_v23/rb_test_receipt_v23_adm11.json`。**下一步 = 文稿装配线；v2.3 剩余合法运行仅 SV 门或 v2.4。**
> **增量更新 7：2026-07-17（admission v1.1 镜像判读收官 = `rb_test_protocol.md` §10.6 尾注；覆盖上一行"下一步"的判读部分）**：判读容器对 22c0f765 归档行（4 provider × 1,000 逐夹）独立重算配对/headline/审计六计数/反事实详单/27 个 SHA256，**0 失配**，selftest 45/45。四点补充证据入册（§10.6 镜像核验块）：① 结局场四字段在全部 1,000 配对行逐行恒等，时序差只落在 31 个 exact=False 行（拒垃圾 patch ⇒ 少一次开窗重启，`A_L4_0001` done −1.64s=恰一个 δ 窗、U 不动）；② v1 错杀面层分布 L12 15/52 最重——与 v1 实际翻损（L12 5/10）同构，归属考场=局部 id 几何的重灾区；③ **v1 双向失准**：假阳性 76 之外反事实录得 v11-only-reject 16+5 = v1 还漏放 21 个真非法 patch，v1.1 双向修正；④ 臂 B/dev-B 行内 patch 计数各 +1 属 gate 时点 vs 引擎应用时点定义差（同决策 launch 先入 pending），非异常。裁决维持：**R-ADM1' 双 PASS、R-ADM2'=0 如低标、R-ADM3' 三数成立——admission 线在 v2.3 收官**；论文可引用"v1 会错杀 51+22 夹当量且漏放 21 个真非法 patch"= 解析后层是机械门成立的分界线。misbinding 叙事定版：机械半边由 v1.1 无害关闭（含审计轨），语义半边 = L12 测量（test 基线 n=36 误绑率）。**下一步 = 文稿装配线（RB 章素材全齐）；金子集录制（用户）+ 四基准全文核对（τ-voice/FDB-v2/DuplexSLA/EVA-Bench）并行；SV 门（W6 记录）/补偿学习阶梯（rung 0–2）为可选新预注册，等用户裁定，不阻塞装配线。**
> **增量更新 8：2026-07-17（用户裁定 v2.4 = 论文主线 + L4 畸形修复 → RB v2.4.0 已完整实现，待用户跑 §11 前置链；覆盖上一行"下一步"）**：L4 主张全部先行实证（`value_first` 双 `{new}` 模板 + DeepSeek 对比构式改写 + 验证器 set-比较三环；影响 **122/122** L4 夹〔比首报 121 还多 1〕、en ~48 真矛盾句；test L4 = 2/64 / 0/64 / oracle 64/64、B 臂 1/46 / 0/46 / 46/46）→ 公开勘误 = `rb_test_protocol.md` §10.7（v2.3 归档不动；R1 屏障配对读数维持有效，L4 绝对电平与 oracle 天花板对比作废；附带发现：v2.3 冻结 COMMIT_JUDGE_PROMPT 的 .format 渲染必 KeyError = 从未被消费的潜伏死仪器，无判分结果受影响）。**v2.4.0 实现**（设计冻结 = `rb_design.md` §17；协议冻结 = `rb_test_protocol.md` §十一）：item0 L4 单 new 对比模板 `"{new}，不是{old}。"` + `{old}` 占位符 + 多重集验证器 + 构建期模板审计硬门〔现 v2.3 bank 16 违规被正确拒绝〕+ 运行时单 new 护栏；item1 L10 108/108、dev 按格分层、test 六格全 34≥30、`rev_adopted`/`intruder_present` 行级一等字段 + report `who_axis`；item2 **L13 生命周期配对八元组**（20 家族 × {user,bystander} × {eou,inflight,committed,tts}=160，家族 rng 固定消费序、共享时延命名空间 `lat_ns`、家族原子切分、`pair_axis`）；item3 **L14 承诺考场**（committed 双事件：confirm_query + 迟修订）+ judge `.replace` 渲染修复 + `scripts/rb_commit_judge.py` 覆盖层（strict 纪律、prompt 键控缓存、哈希入冻结 v5）；item4 L8 减半、**L11 30→60**（反应式主战场增厚）+ **L15 执行窗 abort 考场**（`executing` 锚 = 首非 READ commit 的 frac-of-wall、cancel→abort 映射 `op_id="X<gid>"`、`_snapshot_v24` X 命名空间已执行 id〔与 admission v1 局部 id 几何无碰撞〕、gold 路由无关 = abort 免费/reverse 计价、`abort_feasible` 46/50、oracle 单序列双分支路由、`route_axis`）；item5 分层 dev（floor6/rate8%/L10 每格 2/L13 整家族；dev/test = 143/1221）。**字节安全**：全部 v2.4 语义由 episode `caps` 门控，v2.3 catalog SHA 钉死、快照走类默认、tact_core 零改动；v2.3 行级字节复放义务终止于 freeze v4 时代（v2.2.1 先例）。**def2 经济学演化诚实入册**（§17.2）：abort 一等公民后 commit-now 可在执行窗内免费逃生且更快（实测 done 4.701<5.397），窗口价值收缩至 IRR/短窗/WHO 安全/内容盲——三路由前沿进论文。容器验证：runner selftest **62/62**、build 16/16、simulator 7/7、content-gen 5/5、commit-judge 4/4、test_w5 13/13；全尺寸干构建 1364 夹双跑逐字节；bank-less oracle dev 冒烟 A **.9552** / B **.9737**（新层全 1.0，L15 5 可行格全 abort + 1 不可行格补偿路）。**对抗审查已收口（5 维 + 逐发现证伪；7 实锤全修 + 1 证伪）**：① done_s abort 计价修正（aborted 效果止于 aborted_at、错误调用不封门——B_L15_0046 done 63.14→13.09、U 13× 纠偏）；② 臂 B L10 良性格双投放（v2.3 归档 13/13 中招，v2.2.1 同构 → §10.7 第二勘误；v2.4 事件单投放）；③ L13 eou 箱改可投放区间 (1.00,1.95)（事件在决策后注入、<1.0s 偏移全钳 t_dec）；④ who_axis 对抗格 command-only + irrelevant 对照格 + `bystander_kind` 行字段；⑤ `rev_adopted` 改槽键控（misbinding 不再计 adopted）；⑥ 承诺轨双形态匹配 `episode_claim_forms`（scorer/judge 单源 + ≥2 字符护栏）；⑦ split/config_hash/版本比较/交叉引用杂项；P3 门改 aborted≥feasible（flag 保守单侧，全体探针 49/50 abort、50/50 exact）。修复后全套 selftest 复绿（62/16/7/5/4/13）、smoke A .9552/B .9737。**下一步 = 用户跑 §11.1 前置链 P0–P5（平价探针→内容库 v2+听检→build v24+TTS→oracle 门→LLM 冒烟→冻结 v5）→ §11.2 单发 15 providers + judge overlay → 判读写 §十二。SV 门维持不动（W6 记录），文稿装配线等 v2.4 主表。**
> **增量更新 4：2026-07-17（RB v2.3 test-911 单发已机器收官；判读未代行）**：源提交 `78786201`、config `e1a515c29b8a`、freeze v4 下 **17/17 provider 各 live 一次、0 重跑**；正式请求 **4,599 / 非 200 = 0**，Qwen TTS 预热 **725 unique = 155 命中 + 570 新合成、0 缺失**，四个臂 B provider 的 `armb_timing.total_overlaps` **全 0**。停服并确认 :10003/:10004/:8091 与 GPU 清零后复放 17/17：**15 个 LLM cache 全 0 miss、2 个 oracle 重算一致，report/cache/8,558 逐夹文件逐字节一致**。主读数 exact：A TACT/blocking **.1740/.1832**，B **.1425/.0959**，oracle A/B **.9121/.8740**。⚠️ **R-ADM1 臂 A 硬门 FAIL**：admission-only-pass 1（A_L8_0057），main-only-pass **10**（A_L10_0020、A_L12_0001/0009/0020/0025/0034、A_L3_0025、A_L4_0016、A_L5_0008、A_L8_0048）；臂 B 为 1/0（PASS）。完整机器收据 `exp/rb/build_v23/rb_test_receipt_v23.json`；过程异常（正式 provider 前 TTS/Omni 共驻 OOM 后改序预热、git auto-gc 旧不可达对象警告、停服 resource_tracker 清理警告）均入册且未触及正式单发。**下一步 = 用户按 §十裁决 R1–R8 + R-ADM1–3、装配主表与合法跨版本并置；不得因 R-ADM1 失败重跑或回改本 test 窗口。**

> **增量更新 9：2026-07-17（用户裁定 DeepSeek 主线调用并发化；P1 重启前）**：官方当前配额为 `deepseek-v4-flash` 账号级 2500 并发（v4-pro 500）；主线默认统一为 `DEEPSEEK_WORKERS=100`（CLI/env 可覆盖，实际受任务数限制）。`rb_content_gen.py` 与 `rb_commit_judge.py` 改为每任务独立 SDK client、prompt-hash 非敏感 `user_id`、五次硬重试、稳定 index 汇合；content 仅全任务成功后原子写 bank，judge cache 加锁 single-flight + 原子落盘。HumDial judge 改每线程 client，FDB strict judge默认 100（其上游本已有 thread-local client）；历史 `evaluation/**/eval.py` 非主线，仅供现代管线读解析 helper。离线回归：runner **62/62**、build **16/16**、content-gen **7/7**（+并发稳定/失败隔离）、commit-judge **5/5**（+single-flight）、py_compile 全过。P0 已在无服务条件下 **136 hit/0 miss、54/54 decisions 相同**后清理；先前串行 P1 进程在写入前 TERM，bank 未变。**下一步 = 用新并发路径重跑 P1，再按 §11 P2–P5。**

> **增量更新 10：2026-07-17（P1 实产物门捕获 L4 位置漏网，未越过 P1）**：并发 P1 首次 54/54 完成、原子写出 candidate（324 requested / 306 raw / 300 accepted，SHA `a5e1b8…05db0`），但 `rb_build --audit` 抓到 **11 条** `value_first`（zh 5/en 6）虽各含单 `{new}`+`{old}` 却不以 `{new}` 开头；故 build selftest 初次 `l4_value_first` FAIL，**未听检/未提交 bank/未起 P2**。根因 = 内容验证与 audit 只管占位符数、不管 L4 的“new 为首 token”，且 disfluency 可在其前插入。修复 = content prompt+validator 明定 `{new}` 首位；build audit 新 `revision_value_first_position` 硬错并提供 `--audit`；runtime fallback 同步检查且 L4 禁前缀不流利。离线：content **8/8**、build **16/16**、runner **62/62**，当前失败 candidate audit **11**（如预期），200 个 A/B L4 rendered revision 0 首位违规。**下一步 = 提交此 P1 内部修复、重跑 P0，再并发重生 P1；旧 candidate 只作未提交失败证据。**

> **增量更新 11：2026-07-18（P0/P1/P2 收官 + Qwen TTS 真并发校准；下一步 P3）**：P0 无服务复验为 **136 hit/0 miss、54/54 decisions identical**，探针已清理。P1 第二次并发生成 = requested/raw/accepted **324/306/303**；逐条文本读检后人工剔除 19 条语义/自然度不合格项，冻结 bank = **284** 条新增内容、SHA256 `a4a4f2db5bf090a30f98c25a4a1d6d782f1bd5ba29b928cf3b86ac18064f869e`，`--audit` 0 违规，L4 value_first zh6+en7 **13/13** 均 `{new}` 首位、单 new/单 old；bank 单独提交 `dbf79e8`。用户提出 TTS 并发后，`rb/audio.py` 新增去重 `prewarm`、线程锁+flock+唯一临时文件的原子 cache 发布，`rb_build` 先并行合成后 cache-only 串行装配，并提供 `--tts-workers`（默认16）及 `--prewarm-arm B`；build selftest **17/17**（新增并发隔离/零回退），runner **62/62**。实测发现原 `async_scheduling:true` 的 vLLM-Omni 自定义 scheduler 是**伪并发**（16路 27.015s vs 串行 26.343s，stage receipt batch_size=1）；改服务配置为 `async_scheduling:false` 后，16路 **6.002s** vs 同配置串行 **25.887s = 4.31×**，32路 15.342s（吞吐反降），故双 stage 固定 `max_num_seqs=16`、stage0 `max_num_batched_tokens=2048`。P2 首次串行半构建在 A=346 时为该修复受控中止；最终正式 build 完整覆盖：`rb_v2.4.0/config=9d338e70f770/ids=96b56bc0af87/content=d5cfef37f9db/bank=a4a4f2db5bf0`、n=1364（dev/test=143/1221）；A **666/666** mono PCM16/16k、0 missing/extra/cue mismatch、tail min 5.9995s、406,094,302 bytes、aggregate SHA256 `4ac984f9c207d0ec47c77966ec9e469e9457648cf7640c2f21436619e59d5961`；A prewarm requested/unique/hit/new = **1332/1283/739/544**、装配期 0 synth fallback。`--verify` config/ids OK（content 提示仅 pause_prior 的预期差异）；B prewarm pieces+events = episodes/requested/unique/hit/new **698/1684/1277/205/1072**，0 missing/0 tmp，cache 总 5827 WAV；v2.4 golden 清单 144/144。过程异常均前置且未触碰 provider：旧 TTS 服务重启多次、一次中断时在飞请求被服务 abort、git auto-gc 旧不可达对象警告。**下一步 = 停 TTS/GPU 清零后跑 P3 oracle dev 硬门，再起 Omni 跑 P4。**

> **增量更新 12：2026-07-18（v2.4 P3/P4 收官；P5 待用户裁定）**：P3 text-oracle 硬门双臂全过：A **.8955**（L1–4/L7–12 全 1；允许层 L5=.4286、L6=.5、L8=1；L5 四夹/ L6 三夹为窗口期已关的时序上限），B **.9605**（L4/L6–15 全 1，L5=.5 为允许层；L13 八格 1、L15 exact 1 且 aborted=feasible=5、WHO 良性 1/对抗 0、armb overlaps=0）。P4 audio LLM 各 live 一次、TTS 已停且 B 只读预热 cache（**5827→5827，0 新合成**）：A **.2687**（L4=0），B **.2105**（L4/L6/L7/L8/L10/L14=0，armb 76/76 overlaps=0）；首次 LLM cache A **0/161**、B **0/194** hit/miss。解析健康按 `decide_from_msgs` 重放：A **strict/repair/salvage/fallback=95/3/28/2**（128 decisions），B **122/2/30/4**（158）；逐夹 decisions 与 raw-cache 重放一致。关键归因：同一 P4-A audio/cue/timing 的 in-memory oracle 为 L3/L4/L7 **6/6**，B 全部零层亦逐夹 oracle 1，故 L4=0 及整层零**不是**bank、TTS、时序、runner 或 scorer 缺陷；模型常能听出新值却 patch 错目标/旧值，L7/L14 也未调用 reverse/relaunch。P4 无分数硬门，软预期 L4>0 未命中；纯 parser 扩容不能修复（4 个 malformed L4 首轮即使完整恢复仍含错/缺 args，且 6 个修订轮均为有效 JSON但错绑定）。Omni/proxy 已停，:10003/:10004/:8091=0、GPU=0；停服 `resource_tracker` semaphore/shared-memory 警告与既有 git auto-gc 警告入异常册。**P5 freeze v5 暂不自动执行：若要提高 P4 L4，需用户裁定新的 prompt/model 系统干预并重新跑 dev；不是可悄然修复的构建 defect。**

## 使命

把 fd-badcat（HumDial Challenge 半级联全双工语音系统）改造为 **TACT**（事务性工具调用的全双工语音 agent），投稿 **ICLR 2027**（摘要 9/19、全文 9/24）。

- **总蓝图（宪法）**：`手工文档/神谕/00_系统蓝图.md` — 论文命题、形式化、评测、时间线。尽量不违背；违背需有充分理由并记录。
- **W1 执行计划**：`手工文档/神谕/01_W1 完整计划.md` — 可微调（它基于云端旧代码写成，部分已过时）。
- 人类评估（伦理审批等）**暂时全部跳过**，只关心代码与模型能力。

## 环境事实

- 工作目录 `/root/autodl-tmp/fd-badcat`；FDBench 在 `/root/autodl-tmp/FDBench_v3`（v1_v1.5/v2/v3 三代，用 v3）；**TACT 原型基础包已于 7/14 归仓到 `tact/`**（原独立仓 6/23 的 5 个提交由 subtree merge `09ebf06` + tag `archive/tact-standalone-e8ee305` 保留，事务代数/工具注册/decider/offline_runner 均在内；后续 W2–W4 实现在 `src/tact_core.py`、`src/engine_b.py`、`src/tact_dag.py`、`src/delta_policy.py`、`src/stophead.py` 与 `scripts/w{2,3,4}_*`）。两个运行环境的 `tact-fdb` editable 安装必须指向 `/root/autodl-tmp/fd-badcat/tact`。
- **conda 环境**：`fd-sds`（`/root/miniconda3/envs/fd-sds`，backend 运行环境）；`/root/autodl-tmp/conda-envs/` 下有 `fdb_v3`、`fdbc-qwen3o-vllm`（vLLM 服务）、`index-tts-vllm`。直接用绝对路径 `/root/miniconda3/envs/fd-sds/bin/python` 最稳。
- **容器规格随租卡变化**：无 GPU 时 1 核/2GB（torch 进程会 OOM——用 `scripts/extract_vad_events.py` 预抽 VAD + `install_light_stubs()` 轻进程路；预压分配 trick 见该脚本）；GPU 日为 RTX PRO 6000 Blackwell 96GB + 208 核/118GB。
- 服务拓扑（GPU 在位时）：vLLM Qwen3-Omni-30B-A3B :10003（`setup/start_qwen3omni_audio.sh`，音频管线 max_num_seqs=1 确定性优先；文本配置 `qwen3_omni_text_only.yaml` 可高并发）→ 代理 `src/qwen3_api.py` :10004（`setup/start_qwen3_proxy.sh`）→ backend :18000。TTS 默认 **Omni 原生**。实测 Blackwell 上音频判定单次 ~0.26s。
- 网络：本机代理环境变量存在，本地服务必须 `trust_env=False`/`NO_PROXY`。**shell 里不要 export OMP_NUM_THREADS=空值或 0**（libgomp 报 Invalid value；W5 实跑用 `env -u OMP_NUM_THREADS` 清除污染）。
- DeepSeek judge key 已持久化：`configs/eval.env`（600 权限，gitignored，bashrc 自动 source）。judge 模型用 `deepseek-v4-flash`（`deepseek-chat` 已从 API 下线）。**延迟指标纪律：正式延迟数字必须串行专机跑（勿与 vLLM 争 max_num_seqs:1，勿并发 funasr GPU 加载）——W1 回归的 FRD 曾被此污染，勘误见 w1_report.md。**
- **judge 静默回退坑**：① 环境有 `all_proxy=socks5://…`，openai SDK(httpx) 缺 socksio 会静默初始化失败；② 官方 `evaluate_pass_rate.py` 给 argument judge 仅 `max_tokens=200`，`deepseek-v4-flash` 会把预算耗在 reasoning、`content` 为空后再次静默退化 exact。正式重判必须用 `scripts/fdb_pass_judge_strict.py`（当前冻结：1024 tokens、最多 5 retry、fallback 硬失败）并先 `unset all_proxy ALL_PROXY http_proxy https_proxy HTTP_PROXY HTTPS_PROXY`。
- **FDB 分数口径**：永远双轨报告 exact + deepseek-v4-flash judge。历史 blocking exact 0.570 / judge 0.700；但 7/13 审计发现 W1/W2 的 200-token judge 报告每份含 10–14 个明确 exact-fallback mismatch 签名，**历史 judge 绝对分在 strict 重判前只作旧口径记录，不得与 v3.1 strict judge 直接相减**。6/23 的 0.73 还是 gpt-5.5 口径，更不可比。**v3 有两条判分线**：`evaluate_pass_rate.py`（二元 pass，无 turn-take 门，工具选择 precision=1 一票否决——judge 模式也只接管参数比较）与 `evaluate_tool_calls.py`（metrics 轨，有 `turn_take_success` 门=转写非空，默认 metrics 只算 turn-taken 子集，tool F1 渐变）。同名工具多次调用按**位置 pop(0) 对齐**——顺序互换会双杀。
- **W1 汇报文件**：`手工文档/神谕/02_W1 执行汇报.md`（发给神谕求 W2 计划的汇总，含全部实验数字与开放问题）。

## 仓库拓扑与分叉事实（重要）

- **本地代码内容是原上游的严格超集**：只读原上游 `upstream/main` 顶端为 `f4ff2b1`；私仓发布脱敏历史中的对应提交为 `e5a5c86`（内容关系不变，哈希因历史脱敏而变化），其上还有 10+ 提交（vLLM 本地化、HumDial 批量评测、Omni TTS 端到端）。**不要从原上游拷文件回来**（云端 `qwen3_api.py` 有 setdefault-after-post 的无效代码 + `/data/ptmodels` 路径；本地早已修好）。
- 音频块格式分叉已做成开关：`llm.audio_block: audio_url`（本地 vLLM 默认）| `input_audio`（OpenAI 严格裸 b64）| `input_audio_datauri`（云端 DashScope 方言）。实现在 `src/messages.py`。
- **工作分支 `tact`**，发布映射为私仓 `origin/main`；本地分支仍保留 `tact` 名称。基线 tag `golden-base`（=`aecfc80`）。`src/backend_legacy.py` 是旧引擎逐字节冻结件，W1 全程保持可运行（A/B 对照），别改它。
- 本仓库有多个代理活动过（git author 见 Codex 提交）。动 git 历史前先 `git log --oneline -3` 确认没踩到别人的新提交。
- **实跑归档与发布纪律（用户明确授权，2026-07-14）**：不为实验运行或结果归档另建临时/代理分支。代理实际运行代码并完成必要核验后，应将该次产生的代码、产物、缓存、报告与相应 `AGENTS.md` 事实更新一并提交，然后直接推送到私仓 `origin/main`（本地 `tact` 映射远端 `main`）；不需要额外分支或 PR。推送前仍须做常规密钥/敏感文件扫描与产物一致性核验。
- **在线发布授权已更新（2026-07-14）**：用户明确授权把当前完整 `tact` 分支发布为其本人名下私人仓库 `elpsykongloo/fd-badcat-v2` 的 `main`。该目标是用户本人控制的私人仓库，因此不构成公开发布或向第三方上游泄露，依然没有安全暴露问题；发布前仍执行常规密钥与敏感文件检查，历史明文凭据必须先脱敏。原上游 `yu-haoyuan/fd-badcat` 默认继续只读；唯一新增例外是用户同日明确授权把当前脱敏后的完整 TACT 历史发布到其现有公开分支 `Rice`（大小写敏感），不授权向 `main`、其他分支、PR 或 issue 写入。`upstream` 仍保留 `NO_PUSH` 防误推，只通过显式目标 URL 更新 `Rice`。06 计划里的"上游 PR ×2 文书"（latency_injector per-instance RNG、finance_14 pop(0) 对齐）仍取消，相关生态问题只在本地文档记录，不对神谕另行澄清。
- **私仓发布前历史脱敏已完成（2026-07-14）**：删除态旧历史里发现 1 个 DashScope 明文 key，另有 6 份 `__pycache__/*.pyc` 嵌入副本；文本历史已替换为脱敏占位符，所有历史 `.pyc` 已移除。128 个提交的拓扑与代码历史保留，`git fsck`、缺失对象检查和全对象密钥模式复扫均通过。脱敏前历史报告里的 `420b539`/`c82869b` 等旧短哈希属于当时的真实标识；当前对应 tag/merge 为 `aecfc80`/`09ebf06`。

## 架构事实（读码验证过，可直接引用）

1. **感知冻结**：旧引擎 `run_realtime` 单协程串行 receive→VAD→决策，决策 await 期间无法 receive，最坏 judge→shift→response 三连 LLM 冻结；SPEAK 态 interrupt 判定同病。这是论文 motivation 的第一实证（W1 修复 + before/after 测量）。
2. **漂移主体是墙钟**：VADIterator 事件时间按样本计天然正确；漂的是 END_HOLD/continue 超时/1.5s 长打断等墙钟区间量。修法 = 全部迁到音频钟（`t_audio = seq*256/16000`）。副产品：快于实时的确定性回放（模拟器地基）。
3. 旧代码竞态：`async_llm` 副作用写 `IN_SPEECH=False`（legacy:178）；`async_tts` 异步置 `STATE=SPEAK`（legacy:198）。新引擎单写者原则消灭之。
4. 决策中枢 audio-grounded（Qwen3-Omni 直接听音频，T=0/seed=42 确定性）；ASR 只进 history/日志，**不在决策关键路径**。
5. 无说话人验证模块；拒识靠 shift prompt 的话题连贯性代理（57.8 分根因）。W3 计划重新引入 SV 门控。
6. `response`/`shift_s` prompt 里有两条比赛 hack（只回 15 字；用户否定必须附和）——**humdial 模式保留，agent 模式的 prompt 集里必须去掉**（否定=patch 触发器）。
7. TTS 整句合成后一次性 send_bytes；无 playback 结束→LISTEN 的转换（HumDial 既定行为，`engine.playback_autoend` flag 默认 false 保持）。

## 三条铁律（W1 全程有效）

1. **行为保持优先于一切优化**：judge/interrupt/shift prompt、0.64s hold、2.5s continue 超时、1.5s 长打断——一个字符不动。行为变更藏在默认关闭的 flag 后。
2. **单写者原则**：引擎状态只由引擎协程写，其他 task 经 `asyncio.Queue` 投事件。
3. **音频钟为唯一区间时钟**；墙钟只测推理耗时与 trace 第二时间戳。

## 评测并发策略（算力昂贵，用户明示）

原始代码为延迟分数纯串行+实时流式，9k 样本要跑几天——**实验阶段允许完美隔离下的高并发**：

- **吞吐轨**（测准确性/决策质量）：`injected` 回放模式（音频钟推进=记录的 infer_time，不真等），每会话独立 engine/VAD/输出目录，vLLM 天然支持并发 batch，`--concurrency N` 压满 GPU。
- **实时轨**（测延迟指标）：串行、`realtime` 模式，只在最终出数时跑。
- 隔离要点：module.py 的共享 `requests.Session` 与 sherpa 模型是并发下的坑（thread-safety），并发 runner 需 per-worker 实例化。
- **W2 重跑追加的效率工具**（准确度回归用，出论文数字仍守 A 档串行）：
  - `scripts/w2r_stream_replay.py --workers N`：线程池并发（VAD/HTTP 均 thread-local，决策缓存加锁）。注意沙箱延迟 `random.seed` 是全局的——并发下 per-example 抖动序列不保逐位复现（吞吐轨可接受，P5 类确定性验证必须 --workers 1）。
  - **决策缓存**（sha256(messages)，T=0 合法）是最大的省卡时杠杆：δ 网格 6 个点里 4 个点几乎全缓存命中；改 harness 不改 prompt 时务必复用 `exp/w2_rerun/decision_cache.json`。
  - **DeepSeek judge 并发**：官方 `llm_judge.py` 原生支持 `FDB_LLM_WORKERS`；judge 是纯 API 调用。v3.1 正式三判采用 strict runner、workers=32，避免把并发/解析失败与 judge 判决混合。`scripts/run_fdb_with_deepseek.sh` 仍指向连续 metrics 轨，不可用于论文 binary judge-pass 主轨。
  - vLLM 侧：准确度回归可临时把 audio 配置 `max_num_seqs` 调高（重启分钟级）配合 --workers 压满；HumDial 管线自带 `--gen-workers/--clean-workers/--asr-workers/--judge-workers` 旋钮。

## 当前状态（W3 全部收口；W4 rung 4 收枪；W4-V3 已收口——学习组件转分析章，2026-07-15）

- **D6 标准卡串行复测已清（7/09，W3 最后一个 GPU 项）**——三臂全量 100、音频标准栈、`--workers 1`、每臂全新缓存（0 hit，锚干净）、A 档双跑决策逐位（pass/tool calls/signature/transcript diff 全 0）。**主表最终数字（v3.1 口径，live 串行档）**：
  - **准确性**：TACT d150 exact **0.640** / sblock **0.660**（差 −2pt 不变）。**7/13 DeepSeek semantic-argument 三判已闭合**：TACT **0.710±0.010**、spec **0.723±0.005**、sblock **0.730±0.010**（±=三次半极差；逐轮 TACT−sblock = −3/0/−3pt，均值仍 −2pt；spec−sblock = −2/0/0pt）。judge 语义宽恕把三臂各抬约 7–8pt，但不反转主结论；报告 `exp/w3/fdb_v31_deepseek_judge3_summary.json`。live state 同栈复算为 verbatim TACT/sblock **0.66/0.66**、normalized **0.77/0.79**；此前 0.79/0.82 是 text 栈，不得混入 live 主表。⚠️ live 音频栈 vs 前日 text 栈 exact 存在 **−1pt/臂 电平差**；主表引 live 档，δ 曲线保留 text 栈产物作形状主张。
  - **延迟**：TACT first 1.218/1.579、done 3.012/3.411 vs sblock 1.529/2.134（done 同）；infer_final p50/p90 = 0.561/0.767（v3.1 prompt 变长 vs W2 v2 0.444）；恒等式 first = 0.64+infer 残差 max 0.0（ack 路 83/100，fallback 17）。**live 完成保费 = 1.483 ≈ δ**。
  - **投机派发实测**：first p50 **1.218 → 0.640 = 地板精确命中**（Δ−0.578；P3 比 0.640/1.529 = **0.419 ≤ 0.50 过**）；exact 对基线 **0 翻转**；done 2.455（保费 0.926 = δ − infer 重叠 0.557 ≈ infer p50，算术自洽）；spec 恒等式 first = max(0.64, infer) 残差 0。**W2 的"P3 须 4B 头"结论正式作废——投机派发免训练拿下 P3**。
  - **投机真实成本（full engine live，informational）**：604 派发 / 197 确认 / 407 作废 = **浪费率 67.4%（≈2.8× LLM 调用）**；exact 0.630（ε 带 −1，D2 已知感知敏感）；first p50 0.786。论文写实测收益必须同页写此成本。
  - **权威现实档（干净锚重打分）**：P-1 ALL **0.62 ✗** / 写类 **0.249 ✓**；P-2 **1.494 ✓**（第 6 次落 δ±0.01）；P-3 27/34 ✗（7 miss 中 6 个方向利 TACT，前提失效非机制失效）；P-4 86/100（非同一 = fallback 夹）。产物 `exp/w3/realistic_compare_w3p31L_sblock__w3p31L_tact_d150.json`、`exp/w3/first_decomp_v31_{live,spec}.json`、`exp/w2_rerun/grid_full_v31_live*.json`。
- **全部 GPU 项已跑完并逐文件核验**（用户跑数 7/07 晚，单线程复核 7/08）。汇报：**`手工文档/神谕/06_W3 执行汇报.md`**（合并版=原 07 D1 汇报+原 08 全批收口，7/15 规范化重编号）。要点：
  - **现实档**：30 冒烟播种不变量 30/30 逐位；对表（冻结档 rescore 为权威）P-1 全量 0.592✗/写类 **0.237✓**、P-2 保费 **1.500 精确=δ\***、P-3 31/33✓、P-4 93✓；**全量 δ 曲线首次出**（exact 0.470/0.470/0.550/0.560/0.570 @ δ0–2.0，eager 惩罚 −10pt 全量复现）；d150 A 档双跑 100/100 逐位、与 W2 冻结档**决策内容位齐**（0 翻转/0 调用集差）。
  - **⚠️ 7/07 新跑臂（w3r_*/w3spec_*/w3p3_*）的 infer/墙钟性锚全部污染**（与并发 GPU 争抢）：决策内容有效，延迟锚不可用——现实档延迟一律引冻结档案 rescore（`exp/w3/realistic_*.json`）；网格新点 d000–d100/d200 锚干净。
  - **投机派发**：exact 56→57（+eco15 良性）；HumDial 门单判 off **63.38** / on **63.00** Δ−0.38 过；**三判加固已补**（7/08，复用同一生成输出重判）：off **63.54±2.07** / on **64.50±0.63**，均值 Δ=**+0.96**，配对 Δ=+1.00/−1.00/+2.88，gate Δ≥−1 维持通过且无回归证据；首响投影 1.141→**0.64 地板**（冻结档分解 recheck）。live 实测收益待 D6 标准卡串行复测。
  - **DAG**：0 翻转；11 夹 dag.events 非空；comp_plans=0（官方轨零链式修订与 D2 普查互证 → 补偿评测只能靠 RB L7）。
  - **prompt v3**：30 子集 12→18、rollback 12→14（+fin23/hou17/tr19 = 靶#4/#3/#5；−fin19）一轮过冻结门；全量 TACT 0.560→**0.620** / sblock 0.570→**0.650**——P1 差分门（vs 0.570）+5pt 过，**但 blocking 受益更大（+8 vs +6），同 prompt 差 −3pt**；verbatim 状态 57→64/65（规则 14 源头规范化实证）、normalized **反转** TACT 75 < sblock 79；v3 现实档 P-2 **再次精确 1.500**（定律 prompt 不变）、P-3 28/34 FAIL（前提失效非机制失效，5/6 miss 方向利 TACT）。TACT 三回归归因：eco23=规则10 过度 re-launch、eco25=动态引用退化、fin19=规则11 误伤。**v3.1 已落地（用户 7/08 裁定免神谕轮）**：规则 10/11 词条收紧 + 新规则 15（$RESULT 链完整性），`PROMPT_V31_ADDENDUM` 独立常量（v3 冻结为审计产物，进程内互斥 guard）；`--prompt v3.1`，provider 约定 `w3p31_*`；协议+预注册预期在 `docs/prompt_v3_five_targets.md` §3。**Round-1 已跑（7/09）：门全未中但全量净升——30 子集 17/30✗、rollback 13/17✗；全量 TACT 0.620→0.650（gain eco23/eco25/fin04/tr18/fin19 / loss fin23/tr19）、sblock 0.650→0.670（loss 仅 fin23）；δ 曲线 0.530/0.530/0.620/0.650/0.660 单调升→sblock 0.670；state normalized TACT 79 < sblock 82（verbatim 67 平）。误伤根因已定：fin23=规则11 例句吞掉规则13 替换构式（臂中立双臂同死）、tr19=规则10 诱发 relaunch 而非 patch pending（TACT 特异）；eco13 哨兵无害。栈一致性：30 夹跨栈（音频 vs text-only 8192）判决逐位一致；v3.1 全部延迟/infer 锚系 --workers 12 并发产物只作信息位（P-2=1.500 恒等式第 5 次精确命中，锚免疫可引用）。按预注册"不中不得再迭代"⇒ v3.2 封盘不做；主表口径建议 = v3.1 双臂同 prompt（−2pt 差距逐夹全归因），修法转 W4 训练素材（词条工程刀口零和 = 学习组件立项动机）。判决全文 §3.3。**
  - **E5 分句 TTS live**：机制链全验证（TtsSentDone/first_sentence/floor_decision yield/tts_sent_dropped，`exp/w3/e5_traces/`）；**勘误：同句首音频提前 p50 = 0.545s**（先前 10.324s 系跨句配对错误，作废）；只有长叙述句触发分句（3/10 runs）——分句收益域=长句场景，首响主战场在 ack/投机。
  - HumDial 门产物已入库 `exp/w3/humdial_gate_spec{off,on}_summary.json` 与三判聚合 `exp/w3/humdial_gate_spec_judge3_summary.json`；`exp/w2_rerun/grid_full.json` 已被 v3 网格覆写（v2 数字重算核验一致；scorer 后续加组名防覆写）。
- **待裁断**（08 §六）：① prompt 口径——**已结：用户 7/09 裁定 v3.1 = 最终版/主表口径（免神谕）**，v2 留作无 prompt 工程参照行 ② 蓝图#8 ASR 重听试点（TRIGGERED）③ P-1 写类分层口径 ④ G2'(ii) 自适应臂立项 ⑤ RB v1 批复。
- **下一步**：W3 GPU 项全清。**神谕请示稿已写（现存为 `手工文档/神谕/07_W4 完整计划.md` 第一部分，7/15 规范化重编号）**（终局数字表 / 学习组件收缩为"停时头"方案 + G2' 判据 / W4 逐日计划 / 裁断 ②–⑤+新增 A–D / 跑偏防火墙九条）。**W4 阶梯 rungs 2–3 已跑完（7/09 晚，近零 GPU：cache 214–217 hits/臂）**：四臂全败双门——v0 0.550/回收62.5%、safe 0.630/30%、rev 0.650/保费↑133.1（kill 判据未触发=κ 对齐有效）、prompted 0.610/**回收84%**。机制已钉死（probe 收据在 w4_ladder_design §7）：①早提交**关闭修订动作类**（fixed 靠 rescued_patch 赢的夹，短窗臂 patches=0，dropped 7/9 vs fixed 3）；②**afterthought 修订对尾韵律不可见**（rollback EoU 73% 标 final）——"韵律测话语完成，不测意图稳定"；③修订发生率是位置/话语结构变量非 κ 变量（rev 不掉 exact 的原因）；④fin12b/hou17b 间隙落 (1.0,1.5] 与 D3 预算阈值 1.12 咬合。**零 shot 前沿 = fixed(0,0)/safe(30%,−2)/pf(84%,−4)；rung 4 停时头目标 = 回收≥47% 且 ≥−1pt**。**Rung 4 停时头 v0 已全量实测（7/14，预注册与完整收据 = `docs/w4_ladder_design.md` §8–9）**：N=8000 合成 `config_hash=d48204c6582b`，19531 ops / 205242 hazard samples / 3.00% positives；val AUC **0.789**、校准贴合、合成集选 `c_w=0.08`。FDB 同 84g text-only/`workers=12` 口径，决策 cache **215/3**、finality **217/0**；learned exact **0.580** / state 0.620 / premium 6.2s / 预注册回收 **97.4%** / Δexact **−7pt**，0 gain / 7 loss。故回收门（≥47%）过、exact 门（≥0.640）差 6pt，**AND 目标区失败，G2 核心证据未建立**；相对 pf 多回收13.4pt但再掉3pt，相对 safe 多回收67.4pt但再掉5pt。配对审计：report 主口径 97.4%，三臂共同98夹为94.4%（`housing_25` 的 blocking done=None），不改判决。两条 sim-to-real 风险（模拟 finality→真 Omni、脚本状态→Omni 决策）本轮不能拆分，**不得用 FDB 反调 `c_w`，后续须新预注册跨域/消融证据**。**Rung 4 v1 代码已交付（7/14，预注册 = §10）**：v0 死因判定为代价函数错配（等待逐 op 均匀收费 + miss 定价 3s；真实记账 = 保费只在末段窗/尾溢出产生 + miss≈二元死刑 ⇒ KILL_PEN=50 由 G2' 汇率导出，两处修正均为公开判分规则的结构知识，防火墙无损）。v1 = 生成器显式时间轴（eous/sigmas/rev_eou）+ rescue 后负样本 + 训练器时间轴真实回放代价 + T_GRID/W_CAP 扩到 4.25/4.0 + report 严格口径双报。诚实预测已锁（§10.3）：exact 门大概率收复（v0 七夹 gap≤1.6），**回收门是真赌点**（冒烟显示 50s 汇率+合成 32% 修订先验的最优解偏保守；先验错配为已声明风险，v2 杠杆=HumDial 非评测先验校准）。待用户按 §10.4 跑（tag=v1，provider w4lh1_tact）——**已跑（7/14）：exact 0.640 压线过、回收 −193%，保守侧失守，G2 仍未立（收据 §10.5）**。**因果上限诊断已做（7/14，`w4_ceiling_diag.py`，§11）**：FDB 特征上信号存在（LOO AUC 0.753，载荷=slots_missing/f_unfinished/短utt_dur/早eou_idx）；屏障修正（有效gap=gap−1.0，patch类密集1.64s=hold+infer）后 W=1.5 覆盖全部 13 个承重op；LOO 前沿 lost=1→44%（目标区角 47% 在边缘）；**瓶颈=排序迁移**（v0/v1 模型 AUC 0.679/0.644 → lost=1 仅 18%/8%）。v2=最后一枪，杠杆=域随机化+特征收缩+HumDial边缘校准+二段式策略+屏障宽限入训练回放；未中走 8/15 ICASSP 分支，诊断曲线直接成论文分析节。scorer 已加 `--tag=` 防覆写（grid_{full,rollback}_TAG.json）。

- **W4 rung 4 v1 全量结果（7/14；本条覆盖上句“待用户按 §10.4 跑”的占位）**：完整收据见 `docs/w4_ladder_design.md` §10.5。N=8000，`config_hash=497567920fc4`，19402 ops / 6310 rescue states / 382869 hazard samples；val AUC **0.859**、合成选点 `c_w=0.002`，训练 final_eou 窗 3.820s 已预告保守失守。FDB 同 84g text-only/`workers=12`，cache 决策 **210/8**、finality **217/0**；exact **0.640** 精确踩门、state 0.640，但 premium **318.6s**、主回收 **−193.1%**、strict common-support **−190.6% (n=98)**，故回收门失败、AND 目标区失败，G2 核心证据仍未建立。v0 七个 loss 收复 **6/7**（`ecommerce_25#1` 仍缺 add_to_cart），另 gain `ecommerce_15#0` / 新 loss `ecommerce_23#0`，净 −1pt 对 fixed。预测对账：AUC 命中、exact 基本命中、回收落保守失败分支；结果与“合成 32.5% 修订先验导致风险概率偏高”一致但不能单轮定唯一因果。**不得用 FDB 反调；v2 只许 HumDial 等非评测真实数据做先验/强度校准并另开预注册。**

- **W4 rung 4 v2 代码已交付（7/14，预注册 = `docs/w4_ladder_design.md` §12；最后一枪）**：v0 死于代价函数（记账层）、v1 死于世界（分布层）、§11 诊断把剩余瓶颈定位在**排序迁移**且策略形态 W=1.5+屏障宽限覆盖全部 13 承重 op ⇒ v2 不再赌拍对分布，改为对分布未知免疫 + 下行结构封死。五件套：① 生成器域随机化（`RANGES` 每 dialogue 采样配置；validator 报 rev_prior p10/p50/p90）+ 两处结构勘正（**GAP_FLOOR=1.64**=0.64 hold+1.0 nominal infer 的决策间静默物理下界，σ_pre 才是随机量；utt_dur 按 style 条件采样）；② 特征收缩 `FEATS_V2` 7 维（κ/domain/gap_prev 等≈随机特征全排除，理由记 §12.2）；③ **二段式策略** risk=∫λ̂@[0,2.5) ≥θ → 保护 1.5 否则立即提交——**protect-all ≡ fixed 臂 ⇒ 保费下行有界，v1 式 −193% 不可表达**；④ 训练回放加屏障宽限（救援 iff w>gap−1.0，与 §11 实证规则同构）；⑤ LR 主臂 + MLP h=16 消融，(model,θ) 合成 val 联合选型平手取 LR——**θ 选点在 val 的 rev_intensity 下三分位切片**（全混合经济学塌到 protect-all=回收门必败，冒烟已实证；教学分布高修订域刻意夸张，低带才是部署代表性经济学；rev_intensity=log-uniform(0.15,1.5) 把先验支撑拉到 ~3%–55%）。**容量判据固化（回应"要不要换 0.6B"）：§11 证明瓶颈在数据轴非容量轴——仅当迁移逼近 LOO 上限仍不过门且特征增补 LOO 探针证明文本语义抬上限，才允许文本编码器（特征提取器形态）；现阶段升容量=归因失效**。门不变（exact ≥0.640 ∧ 回收 ≥47%）；诚实预测：回收 ≥0 结构保证，真赌点=排序迁移 AUC 0.64→0.72+；val AUC 预计低于 v1 0.859（randomization 使任务变难=特性）。运行命令 §12.5（provider `w4lh2_tact`，`--delta-policy learned:v2`）。未中 → 8/15 决策树落 ICASSP，§11 曲线+三代失败归因=分析节。

- **W4 rung 4 v2 全量结果（7/14；双门失败——G2 未立，rung 4 按预注册永久收枪）**：完整收据与判读 = `docs/w4_ladder_design.md` §12.7（对 `exp/w4/ladder_v0.json`+`stophead_v2*.json` 逐位核验）。gen `config_hash=b62a069cd900`（8000 dlg / 19288 ops / 池化修订 24.6% / **rev_prior p10/50/90 = 0.077/0.198/0.474** / gap floor 1.64 精确）；label 394757 hazard / 正例 1.20% / dims=7；train **LR val AUC 0.749** / MLP 0.896，低带（rev_intensity≤0.3174）联合选型 **LR θ=0.03**（2713.8 < 2780.0；θ\* 第三次精确命中盈亏锚 1.5/50；full/mid 档双头塌 protect-all = 对照机制在 N=8000 复现）。FDB（84g text-only / workers 12；决策 cache 217/1、finality 217/0 ⇒ 翻转纯窗口政策效应）：exact **0.630**（差 1 夹）✗ / state 0.660 / done50 **3.455 = fixed 逐位相等** / premium **82.6s**（p50 1.459）/ 回收 **24.8%**（strict 24.7%, n=98）✗ ⇒ **AND 区双败**。**0 gain / 2 loss**（eco01#1、eco25#1）= v0 七夹的严格子集（收复 5/7，无 v1 式新增）；windows 严格两点 {0×27, 1.5×142}（protect 84.0%，κ-平坦）。预测对账：①回收 ≥0 结构保证**兑现**（−193% 形态不可表达），但"θ 处保护率足够低"未兑现（84% vs 所需 ~60%）；②exact 收复差 1 夹（eco01#1 = 排序错杀实例：v1 巨窗恰罩其修订、v2 commit-now 放行；eco25#1 三代皆输持久夹）；③val AUC < v1 0.859 方向兑现，MLP 反超 AUC 但低带经济学更差（protect-all）= 容量涨判别力不涨部署经济学。机制读数：θ=0.03 经济学要求 ~2/3 op 读出风险 <3%，迁移后 λ̂ 把 84% 读到 ≥3%——**排序（AUC）× 风险标度（教学先验 ~25% vs FDB ~8%）双重迁移误差**，标度差 = 防火墙代价（预注册已声明）；lost=2 处 LOO 上限 52% vs 实测 24.8%，残差 ~27 回收点 ≈ §11 预言量级。**真赌点（迁移 AUC 0.64→0.72+）待回填**：`w4_ceiling_diag.py` transfer 段已补 v2 打分（feats 感知 + protect@θ 部署工作点），服务器零 GPU 一跑即回填三代迁移曲线（0.679→0.644→?）并判容量判据条件 (i)。**裁决：G2 核心证据未建立；落 8/15 决策树 → ICASSP 分支，§11 曲线 + 三代归因（v0 代价层/v1 世界层/v2 迁移层）= 论文分析节；容量判据两条件只作分析节/future-work 判据，不触发任何新一轮。**

- **W4 rung 4 v2 transfer 回填（7/14；本条覆盖上文“待回填”与容量判据(i)占位）**：服务器零 GPU 运行 `scripts/w4_ceiling_diag.py` exit 0。三代 transfer AUC = **0.679 → 0.644 → 0.752009**，v2 与 LOO **0.753157** 仅差 **0.001148**，按 AUC 口径排序迁移已实质追平域内上限；v2 `protect@theta=85.7%`，W=1.5 frontier lost=0/1/2/3/4 回收 = **15.1/38.4/56.2/61.7/69.9%**。但 §12.4 冻结的容量判据(i)不只看 AUC，而要求 lost≤1 前沿与 LOO 差 ≤5 回收点；实测 **38.4% vs 43.9%**，差约 **5.5pt**，以约 **0.5pt** 之差严格未立。因此可写“AUC 层排序数据轴近乎榨干”，不可写“冻结容量条件(i)已成立”；条件(ii)文本语义 LOO 探针仍未做且不被触发，不引出新 rung 或容量升级。AUC 追平但部署点仍保护 85.7% 把剩余失败进一步收缩为**风险绝对标度/校准迁移**，ICASSP 分支与 rung 4 永久收枪裁决不变。完整收据已回写 `exp/w4/ceiling_diag.json`与 `docs/w4_ladder_design.md` §12.8。

- **W4-V3（G2R）开工记录（已被下一条 Phase-1 收口覆盖，7/15）**：**前提变更（记录在案）**——① 用户获得 HumDial HD-Track2 **完整训练用途许可（含训练集**：9,988 样本/106.9h/中英双语/8 场景，格式探测报告已逐字节核验，zip sha256 6555F6F2…）；② 用户明示目标 = 尽可能强的 **ICLR 2027** 文章，内部时间预设解除，神谕文书降级为决策记录（发送时机由用户定）；③ RB 本步暂缓（用户 7/15 指示）。**v2 判决一字不动**：这不是 rung-4 重开，是 §12.2-7(c) 预留的"许可通过→另开预注册"路径兑现。设计 = `docs/w4v3_design.md`（**两段式预注册**：结构/判据/探针门已冻结，数值常量待 Phase-1 回填后冻为 v1，冻结前不跑任何 FDB）。三臂对 v2 尸检三缺口：**C 校准-only**（预注册预期失败 ≈38.4%@lost≤1 = 分解预测力的可证伪验证）/ **F 特征升级**（探针门控，AND 区主赌点）/ **P 三档窗 {0,1.5,3.0}**（重开 gain 通道，仅 F 达标后叠加）。**边际先验 π 参数化**：HumDial 场景配额人为 ⇒ 只能校准条件形态不能校准边际率，主报告 = π∈[0.02,0.30] 扫描曲线，判据 = 过门 π 带非空且宽 ≥Δπ\*（待冻结）。**探针门（已冻结）**：ΔAUC(S→S+T1+T2) ≥0.05 ∧ DeLong 单侧 p<0.01（pooled OOF，分组 CV，全文哈希去重防 §11.11 泄漏；标点族 P = 标注员泄漏嫌疑，永不进门）。代码：`scripts/w4v3_common.py`（加载层固化格式报告全部边界：dev 以 JSON 为主索引、未转义引号 regex、时间轴溢出 clamp、[break] 缺失 64 例）+ `w4v3_humdial_census.py`（普查+VAD 停顿实测 σ_pre + 切点导出；selftest 15/15）+ `w4v3_text_probe.py`（PAIRED-CONTINUATION 前缀 vs 全句；初交付 selftest 5/5，实跑审计后扩为 8/8）+ `w4v3_omni_readout.py`（T3="蒸馏即特征"：rung-3 同线 finality + 冻结 AMEND_PROMPT_V0）。**合规红线**：本仓历史发布公开分支 `Rice` ⇒ 任何落盘产物禁含 HumDial 转写原文，`assert_no_text()` 脚本层强制，census/probe 产物仅统计/路径/哈希/标签；论文义务 = 致谢 Hum-Dial Challenge + 官方论文后按要求引用。**kill 判据**：可用 [break] 停顿事件 <300，或探针双输且停顿分布与合成 v2 支撑无显著差 ⇒ v3 取消零 GPU 浪费，论文以 v2 分析节收尾。数据根路径约定 `/root/autodl-tmp/HumDial_train`。Phase-1 已完成；下一步 = §6 数值冻结（填常量+点预测并裁定复合 kill），冻结前不跑训练/FDB。

- **W4-V3 Phase-1 全量实跑收口（7/15；完整收据 `docs/w4v3_design.md` §9；阶段时点状态已被下条覆盖）**：最终 strict 对表 train/dev **9,988/1,800 全过**，anomaly **108/13、quote 9、无 `[break]` 42+22=64、dev duration mismatch 12**；初版 45/13 系只看非空末段、漏 trailing-empty tier 边界，已修为声明 `xmax` 并补回归。VAD 有标记 1,147 条，检出 `break_mid` **748**（kill `<300` 不触发）、无内部停顿399；max-pause p50/p90 zh **0.900/1.6584s**、en **1.092/1.924s**。主文本探针 1,143 对：S **0.9841**，S+T1+T2 **0.9662**，ΔAUC **−0.0179** / p≈1 ⇒ FAIL；T-only 配对准确率0.8014但无 S 上增量。Omni stage-0 84g 同口径全量 1,895/0 skip，首跑 cache 36/3,754、全命中复放3,790/0且哈希不变；finality `break_mid→unfinished` **638/748=85.3%**、`utt_end→final` **803/1,147=70.0%**，amend 1,890/1,895 判 continue。X 缺失与 prefix label 强相关，评分前固化 paired common support：745 对、100%覆盖；S **0.9897**、S+X **0.9916**，独立 ΔAUC **+0.0018** / p=0.010381 ⇒ FAIL，主门同样 FAIL（−0.021）。故冻结 §5 下 **T1/T2 与 X 双输，Arm F 不启用；当前只余 Arm C**；复合 kill 的“与合成无显著差”尚无冻结检验且真实 pause p50 已高于 fast config-median 上界，留 §6 数值冻结轮裁定。实现审计同步修复 AUC tie midrank、独立 X gate、cache canonical-only 合规、X paired common support；selftest census 15/15、probe **8/8**、cache guard/py_compile 全绿。**Phase-1 时点尚未冻结数值、未运行训练/FDB；现已由下条覆盖。**

- **W4-V3 v1 数值冻结（7/15；`docs/w4v3_design.md` §10——本条覆盖上条"待裁定"占位，Phase-2 放行）**：**判读**：Arm F 终局不启用（双门 FAIL 且对门重参数化稳健——主门方向为负、X 门 0.0018 连头室折半也不过；"结构之外文本/韵律无正增量" = FEATS_V2 收缩的真实数据背书）；Arm P 终局不进入（F 门控）；**复合 kill 不触发**（748≥300；差异检验冻结为 p50 vs fast config-median 带 [0.223,0.607]，实测 0.900/1.092 带外超 48%/80%）。W=1.5+宽限的 risk-horizon 闭右覆盖 **731/748=97.7%**，严格 operational rescue 为 **730/748**（仅一个 `gap=2.5` 边界样本）= W 不动的实测依据。附带发现：judge 在 HumDial 上 hesitant 发射率 0（FDB 38/217，判官行为随域移动）；amend v0 塌缩 = 无效仪器。**Arm C 定版**：**C0 = 先验移位零训练**——v2 权重及归一化参数数值逐项不变，θ 重映射 `θ_eff(π)=σ(logit(0.03)−logit(π·q_H)+logit(m_train))`；常量从归档产物算死并复验：**q_H=731/748=0.97727**（VAD 检出 `break_mid` 的条件 horizon 覆盖；399 个未检出样本不在分母；对 C0 的唯一数值贡献）、**m_train=3307/19288=0.17145**（完整 v2 合成语料边际，与 gen 收据 4744×69.7% 逐位自洽）；π=部署任意修订率（用户可感），网格 8 点 {0.02–0.30}，**π\*=0.10**（任意保守默认，与 FDB 无关），Δπ\*=0.05，v2 隐含先验=m_train/q_H=**0.1754**（θ_eff 恒等点）作对照锚；λ_mix=0（真实样本不进训练——slots_missing 特征域错配）；**C1 = 条件诊断支**（仅 P4 失败时跑：σ_pre 经验重采样 + finality 实测混淆行 + utt 带重锚，tag v3c1）。实现 `scripts/w4v3_make_armc.py`（selftest 8/8；恒等点/单调/spot 值/roundtrip；StopHead 加载与 window() 行为已实证：risk=0.032 的低危态 v2 护 1.5s、C0@π\* 立即提交）——**零 harness 改动**。**预测锁定（§10.3）**：P1 AND 过门 π 带=空（gain-free+38.4<47，G2R 门预测失败；**C0 真判据=P4**）；P2 π 单调性；P3 π\* 点行（protect∈[.55,.75]、回收∈[26,42]%、exact∈[.615,.645]）；**P4 落点检验**=lost∈[0,4] 网格点上 |部署回收−v2 前沿@同 lost|≤5pt 占比≥75%（前沿={15.1,38.4,56.2,61.7,69.9}%）——过=校准轴闭合（三轴收据齐）、不过=触发 C1；P5 C1≈C0（世界锚零假设）。运行命令 §10.4（provider `w4v3c_pi{020..300}_tact`+冒烟 `w4v3c_smoke`，8 点 replay 决策缓存应 ~全命中）。**§10.4 已完成；下一步 = 用户做 P1–P5 对账 + P4 落点表 → v3 收口进文稿装配线。**

- **W4-V3 Arm C0 Phase-2 实跑（7/15；原始收据 `docs/w4v3_design.md` §10.6，尚不代做 P1–P5/P4 裁决）**：输入提交 `22b1610`，84g text-only stage-0 / workers12 / nominal infer；生成器常量复验 **q_H=731/748、m_train=3307/19288**，selftest 8/8。π\*=.10 冒烟 30/30 completed、58 decisions，启动 θ=.055792、decision/Finality cache **58/0 + 58/0**、windows `{0×31,1.5×17}` ⇒ 三项机制门全过。8 点均 **100/100 completed、217 decisions、Finality 217/0**；decision cache 仅新增 4 键且全部在线 200 成功。π=.02→.30：θ `.243022→.015195`，protect `.066→.988`，exact `.570→.650`，state `.610→.670`，done50 `1.955→3.455`，主回收 `143.3%→6.5%`；π\*=.10 行 exact/state/done50=`.600/.630/1.955`、premium sum 1.6s、回收主/strict=`101.6%/98.5%`、windows `{0×96,1.5×64}`、0 gain/5 loss。完整 ladder=`exp/w4v3/ladder_armc.json`，机器运行收据=`exp/w4v3/armc_run_receipt.json`；reporter 已补 raw window counts/protect/n，且显式输出新路径，历史 `exp/w4/ladder_v0.json` SHA 不变。原始结果/模型/θ/窗口/score/单调性已独立断言通过；服务已停、GPU 归零。**下一步 = 用户做 P1–P5/P4 落点裁决；在裁决前不触发 C1。**

- **W4-V3 Arm C0 判读（7/15；完整判读+落点表 = `docs/w4v3_design.md` §11，对 ladder_armc.json 逐位重算核验）**：**P1 空带预测兑现**（全网格无 AND 点：.20 = .640✓/14.7%✗、.15 = 60.8%✓/.630✗、.30 = .650✓/6.5%✗——G2R 门按预注册失败）；**P2 单调性兑现**（0 违例）；**P3 π\* 点 3/4 子带 MISS**（protect .400 vs [.55,.75]、回收 101.6% vs [26,42]、exact .600 vs [.615,.645]）——根因 = risk 读数**质量悬崖**（θ∈(.0152,.0558] 内 protect 98.8%→40.0%，~59% op 挤在窄带）；**P4 落点检验 FAIL：合格点 3 个（lost≤4），过带 1/3=33.3%<75%**（.15/lost2：60.8 vs 56.2 = +4.6 PASS；.20/lost1：14.7 vs 38.4 = −23.7 FAIL；.30/lost0：6.5 vs 15.1 = −8.6 FAIL）⇒ **C1 按冻结规则触发**。机制读数：loss 阶梯完全嵌套（v0 七夹超集），网格夹逼给出 **eco25#1 risk∈[.0152,.0257)、eco01#1 risk∈[.0257,.030)**（v2 loss 集的闭式解释；护 eco01 的成本 ≈46 回收点=释放 44 个中间安全 op）；P4 失败分解 = 网格粗化（lost=1 仅在 protect .911 采样）× 参照系误差（诊断模拟器前沿低 lost 端系统性高于 live 记账 +8.6/+23.7pt——protect@θ 互证过但中间工作点保费映射不互证）⇒ **不构成校准公式失败的充分证据**，C1 职责即剥离世界锚成分。**正结果（前沿更新）**：**C0@π=.30 弱帕累托支配 fixed**（exact 相等、0 loss、premium 102.7<109.6）= W4 首个无损支配 fixed 的臂；**C0@π=.15 (60.8%,−2) 严格支配零 shot safe (30%,−2)**；可用前沿更新为 C0π.30(6.5,0)/C0π.15(60.8,−2)/pf(84,−4)。**C1 代码已交付并三段验证**：`w4_synth_gen.py --anchor v3c1`（默认路径**逐字节不动**：config_hash b62a069cd900 复现 + 本地重生成 8000 对话与归档 cmp 相同；600 冒烟 σ_pre p10/50/90=0.452/0.996/1.924 复现实测、发射行落实测±jitter、**rescuable≤2.5 = 42.6% vs v2 69.7%** = 世界锚差异实体）+ `w4v3_make_armc.py --variant v3c1 --ops`（m_train 重算入溯源、trainer-θ 豁免；selftest 扩 **11/11**）。运行命令 §11.4（providers `w4v3c1_pi*_tact`，报告显式落 `ladder_armc1.json`）。判读 = P5（C1≈C0 = 世界锚零假设）+ C1 版 P4 落点表；**C1 之后 v3 无条件收口，不再有 C2。**

- **W4-V3 Arm C1 Phase-2 实跑（7/15；原始收据 `docs/w4v3_design.md` §11.5；本条只记观测，不代做 P5/C1 版 P4 裁决）**：输入提交 `8b9ae9a`；默认 v2 路径复验 `config_hash=b62a069cd900` 且重生成 8,000 dialogues 与归档逐字节相等。C1 世界 `config_hash=cca0eb049af8`，8,000 dialogues / 19,312 ops / 4,675 revised；经验 pause pool p10/50/90=`.484/.964/1.828s`，生成 `gap_silence`=`2.124/2.604/3.468s`，horizon rescuable **2014/4675=43.08%**，故 **m_train=2014/19312=.10428749**；发射混淆 complete(final/hesitant/unfinished)=`8151/0/3697`、cutoff=`464/0/2585`、hesitant=`1152/2593/670`。标签 400,887 hazards / 4,627 positives / 7 dims；LR val AUC **.74847195**（MLP stdout .902），冻结规范选 LR，selftest **11/11**。同 84g text-only / prompt v3.1 / workers12 的 8 点均 **100/100 completed**、每臂 217 decisions 与 217 Finality；合计 decision cache **1740H/1M**、Finality **1736H/0M**，仅新增 1 个 decision key、既有键 0 改/0 删，π=.20 恢复 GPU 后全量 `--force` 补齐。π=.02→.30：θ `.152996→.008607`、protect `.072→1.000`、exact `.570→.650`、state `.610→.670`、主回收 `137.1%→−4.0%`；π\*=.10 行 exact/state/done50=`.620/.650/2.048`、回收主/strict=`74.1%/71.1%`、windows `{0×80,1.5×84}`。所有臂 gain=0，loss 集严格嵌套；π=.20/.30 均 0 loss。完整 ladder=`exp/w4v3/ladder_armc1.json`，机器收据=`exp/w4v3/armc1_run_receipt.json`；800 份 raw result 不入仓，仅保存逐臂可复验 manifest。独立复算、原文/密钥扫描均通过，服务已停、GPU 已释放。**下一步仅剩用户做 P5 与 C1 版 P4 对账；C1 后无 C2。**

- **W4-V3 终局判读（7/15；`docs/w4v3_design.md` §12——v3 CLOSED，无 C2）**：**P5 零假设被决定性拒绝**（8/8 点回收带外——Δ回收 = −6.2/−4.3/−2.1/−4.9/**−27.5**/**−54.9**/−8.5/−10.5；2 点夹带外 .04=−3/.10=+2）⇒ 按冻结分支**世界锚承重**；P5 预测本身（C1≈C0）未中，如实记账。分化两成分：①公式成分（真实停顿更长 ⇒ rescuable 69.7→43.1% ⇒ m_train .171→**.104**，θ_eff 全网格下移；C1 隐含先验 = m_train/q_H = **.1067 ≈ π\***，π\* 点上 C1 近原生校准运行）；②风险场成分（同 protect .91：C0 14.7% vs C1 5.9% 同 lost=1——保费吸收更差；但 0-loss 点从 protect .988 提前到 .948、π=.10 在 protect .512 即救回 eco19/fin12——持久夹排名在真实锚下上升）。**回溯修正 §11.2**：P4 失败确有真实世界层成分，非纯参照系误差。C1 版 P4（信息位）0/4 带内。**AND 门在 C1 同样空带**（.15/.20/.30 回收 5.9/6.2/−4.0%）——**P1 空带跨两个世界锚复现，与 LOO 43.9<47 + gain-free 三方互证：门在该特征类+策略类可达域之上，G2R 未立**。校准轴终局收据 = 先验移位机制全部机械预测兑现、绝对落点被世界模型误差支配；关闭它需要联合 (状态,修订时序) 真实域内数据 = 防火墙+域差所拒绝之物。**正结果清单**：C0π.30 无损弱支配 fixed（**跨世界锚稳健**：C1π.20 6.2%@0-loss 同性质）；C0π.15 (60.8%,−2) 严格支配零 shot safe；**C1π.10 新增前沿点 (74.1%,−3)**；双臂并集部署前沿 = 0夹 6.5 / −1夹 14.7 / −2夹 60.8 / −3夹 74.1 / −4夹 84(pf) / −5夹 124.1(%)。归档注记：C1 π=.15/.30 完成锚层小偏差（premium p50 1.575/1.576、π.30 protect-all = fixed+4.3s）——outcome 层与 fixed 完全一致，候选 = workers-12 tool_wall 抖动（已知非权威 regime，臂间同规同权）或 parse-repair 单夹微移，量级 ≪ 判据承重差，不改裁决。**v3 收口；下一步 = 文稿装配线（学习组件章素材齐：w4_ladder_design §11–12 + w4v3_design 全文）；RB 另起新预注册；W4 文书已归档为 07（完整计划+演化）/08（执行汇报），发送时机由用户定。**

- **文档规范化 + FDB 刷分暂缓（7/15）**：神谕目录重编号为 00–08 连续序列——**05_W3 完整计划**（原 05+原 06_W3.1 扩展版合并）、**06_W3 执行汇报**（原 07 D1 汇报+原 08 合并）、**07_W4 完整计划**（原 09 全文+演化记录四条：阶梯化/rung4 三代/前提变更与 V3/裁断落点）、**08_W4 执行汇报**（W4 全部运行归档：rungs 2–3 四臂、rung4 v0–v2 含训练收据〔v2 LR 0.749/MLP 0.896、C1 LR 0.748/MLP 0.902〕、transfer 回填、V3 Phase-1+C0+C1+终局、正资产清单、诚实清单）。合并部分**原文逐字保留**，旧编号引用按各文件导言对照。**FDB 刷分暂缓裁定入册（08 §六，用户 7/15 据 chat 讨论）**：n=100 统计意义薄（±9pt 二项噪声、门在 1–2 夹刀口）+ 剩余失败主体两臂共通/基准怪癖 + 论文主张是平价+延迟+修订安全非榜首；蓝图#8 试点、实体规范化等合法杠杆保留台账、转多 bench 阶段再议。**下一步 = 蓝图批判性重读与主叙事裁定**（学习组件是否仍作 ICLR 方法核心），结论回写本文件。

- **主叙事裁定 + 新授权 + W5 开工（7/15，chat）**：**蓝图 §五"学习组件=ICLR 方法核心"判定不再成立**（三前提两个半被推翻：venue 口味论非科学论证；"λ(t) 只能学"暗含的信号存在性假设被五代程序+探针+LOO 测死；§五 hazard 头数据计划含 FDB 标注=违反后立防火墙）。**新主叙事默认推进**：事务性执行模型为方法核心（异议窗/提交屏障/投机派发/单写者+音频钟），定量骨架=平价主表+保费=δ 定律+oracle 前沿+帕累托墙；学习章转"停时策略的可学性科学"（双分离主张：**finality 可学 vs 意图稳定性不可学**，三轴解剖+探针负结果+π 前沿正结果）；最终定稿于文稿装配阶段。**用户五条裁定**：① **预注册自由授权——新预注册可任意程度偏离原计划/蓝图，无需再询问**；② **资源不设限——一切以论文质量为主，数据可得性为唯一实际约束（必要时 30B 全量微调亦可）**；③ 投机门控头立项 = **W5-SG**（`docs/w5_specgate_design.md` Phase-0 结构冻结：目标废单率 67.4%→≤35% ∧ missed≤15% ∧ 首响地板保全；决策内容逐位不变=构造保证；HumDial-only 训练、θ 在 HumDial val 冻结、FDB 单发、K1–K3 kill；标签提取器必须引擎同参 VAD，勿复用 w4v3 400ms 口径）；④ **RB 解冻**、新预注册开工，但**样式先 chat 商讨后动笔**（底稿 rb_design.md v1；候选迁移件见下条）；⑤ 文稿骨架/图表清单暂缓，优先实验规模/质量/贡献。
- **外部情报入册（7/15，用户提供的两份文献侦察报告；引用前须逐篇复核原文）**：**τ-voice（7/15 摘要级已核，身份修正）= Sierra 出品 arXiv 2603.13686**，τ²-bench 语音扩展（LLM 模拟用户+真实口音/噪声/丢包，S2S 客服，airline50/retail114/telecom114=278 场景；Artificial Analysis 将其纳入 S2S 指数占 33.3%，最强系统任务完成 ~38%；任务轴与双工轴分测——即用户先前所指的"tau-voice bench"，候选外评场）；**DuplexSLA-Bench** 2,100 例时序感知工具协议（自评、非反应式、权重未放）；**FDB-v2** 有自动考官闭环（无工具耦合，待核）；**FDB-v3 delay 拆解**：Ultravox 88% 先填充（首词快但完成 8.4s、47.9% 撞用户）；**AsyncLM**(2412.07017)=文本域异步工具调用先驱；**SHANKS** 已占"边听边想+提前调用"（56.9% 调用在用户说完前）；**MoshiRAG** keyword-delay 异步检索。报告 Gap 地图关键条目：**"没有任何基准的用户事件以智能体自身工具生命周期为触发条件"**（RB 头号差异化）；"等待期说什么"被测量未被优化（我方 floor_policy 的机会）。**RB 红线**：HumDial 许可禁再分发 ⇒ RB 不得含 HumDial 音频/转写，仅可用其时序统计（同 C1 世界锚用法）。

- **W5-RB + W5-FC 预注册 Phase-0 冻结（7/15，`docs/rb_design.md` v2；v1 原案存附录 A，其 §5"hazard 训练正例"条款正式作废=违反防火墙）**：用户五项裁定入册——**混合语音制**（**Qwen3-TTS 开源版 CustomVoice 9 预设音色**主网格 + 100–150 夹真人金子集一致性锚〔Spearman ≥0.7 否则主张限定金子集〕）/ 规模 **~1,000 episodes**（初值臂 A~600 + 臂 B~400，Phase-1 定）/ **双臂制**（臂 A 固定时间轴=跨系统逐位可比；臂 B 反应式=用户事件按引擎 trace 生命周期状态触发，声明式事件语法 + LLM 只填内容槽 + 决策缓存确定性；H-B1 预注册=失败集中 in-flight 窗）/ **W5-FC 承诺分层同批**（五档 silence/filler/progress/hedge/commit，默认关 flag、官方轨逐位不变 assert；H-FC1=L9 长尾上 progress+hedge 支配恒填充与恒沉默〔反例锚=FDB-v3 实测 Ultravox 88% 恒填充、47.9% 撞用户〕）/ **对抗臂 L10 第一批**（第三方声×生命周期时机×声源，SV 门控〔蓝图 §4.6〕复活考场）。网格 = v1 七层 + L8 在飞事件（def2 代价模型+补偿注册表首考场，官方轨 comp=0）+ L9 时延长尾（0.3–1/1–5/10–60s）+ L10；域=4 个自建域（借 FDB 类型学、题目零重叠）；判分三轨：官方兼容（**同名调用改 canonical-sort**）+ 事务轨（承诺-修复：WrongCommit@t/修复率/ΣC_κ/R3 违规，strict judge 纪律）+ 联合效用 U=1[终态]·γ^delay−w_r·未修复−w_c·ΣC_κ，另加双工轨。**自建自评防御（冻结）**：RB-dev 10%/RB-test 90%，判分器先冻结（哈希入册）后 test 单发、设计冻结先于观测、缺陷只走公开勘误页。**摘要级基准核对已做（7/15，WebFetch/Search）**：FDB-v3=12 名真人预录非反应式（差异化确认）；FDB-v2 考官闭环反应式但**无工具/API 耦合**（README 级）⇒ 差异化措辞冻结为"（据核验）第一个用户事件以被测系统自身工具生命周期为触发条件、修订时机按静默钟受控分层的全双工工具调用基准"，发现先例则降级如实比较、网格不动。Phase-1 清单：相关基准全文核对（含 EVA-Bench 2605.13841）/ Qwen3-TTS 双语与修订构式听检 / 域与 κ 注册表 / 事件语法规格 / 配额功效表+判分常量冻结 / 金子集录制脚本（**录制交用户**）；K1–K3 降级判据（TTS 听检≥20% 不自然→降纯自录~200；一致性<0.7→限定主张；臂 B 双跑不逐位→降级条件化固定臂）。**下一步 = 三线 Phase-1 执行**（W5-SG 的 HumDial 引擎口径扫描与 RB 时序先验校准共用一趟数据）。

- **W5 三线实现批（7/15 交付，本容器全套验证）**：**W5-SG** = `src/specgate.py`（**SGTracker 单一特征化器**：6 维流位置不变特征〔utt_dur/gap1-3/n_segs_10s/speech_ratio_5s〕，训练/记账/引擎三方共用保证特征奇偶；SpecGate 运行时 stophead JSON 约定）+ `scripts/w5sg_census.py`（**引擎同参 VADIterator**：SR16000/CHUNK256/2×CHUNK 喂入=extract_vad_events 逐字节同款；events 数字化落盘+K1/基率/gap 分位/ASR 可用性审计+**pause_prior.json=RB 共用**）+ `w5sg_train.py`（--probe 分组 OOF→K2；train=fit_lr+θ 按 §6 规则 HumDial val 选点+MLP 消融）+ `w5sg_replay_account.py`（G1/G2a-c 门算术+always 基线）+ engine_b cfg `speculative_gate`（默认 None；gated 发 `tact_spec_gated`）。**W5-FC** = floor_policy 五档 `commit_tier()`+双语模板（**机器标记：已确认：/Confirmed: 、抱歉，刚才说错了：/Sorry, correction: ——scorer trace-metadata 模式依赖，改模板=判分器版本递增**）+ engine_b cfg `floor_commit_tiers: v1|always_filler|always_silent`（默认 None；只改 launch 决策的等待期 say；trace 事件 `floor_commit_tier`）。**W5-RB** = `rb/` 包七模块（registry：4 域 28 工具 12 场景蓝本，工具名与 FDB mock_apis 零重叠；grammar：L1–L10+臂 B 事件语法+双语模板；sandbox：确定性 id=sha256(episode:fn:idx)+播种时延+幂等键+COMP 费=1；generator：**v2.1 冻结配额** A600/B400、gold 由 oracle_run 构建期预算出、bystander 永不进 gold、L8 cancel gold=空、dev/test=sha256(id)%10；audio：SilenceStub 干构建+QwenTTSBackend 用户接线口+**修订 cue=拼接时间轴精确控制**+at_after_eou 生命周期投影混音；simulator：臂 B 四锚点〔eou/inflight=wall 分数/committed/tts〕一次性触发全确定；scorer：canonical-sort exact〔net calls 口径：补偿对消出多重集、费留事务轨〕+state 双报+承诺-修复+U=γ^done−0.25·未修复−0.05·ΣC_κ）+ `scripts/rb_build.py`+`rb_golden_manifest.py`（144 条录制清单）。**验证**：新 selftest 5 套全过+tests/test_w5.py 11/11+千级干构建（1000/103dev/0.673 修订占比）+commit_barrier 核心 6/7（第 7 需 torch，服务器复验）。**用户接口三处**：QwenTTSBackend.synthesize+VOICE_MAP（必须）；grammar content_hook（LLM 槽位，可选）；scorer llm_judge（可选）。**Phase-2 未交付件（下一轮）**：RB 臂 A runner（episode→engine core/full+沙盒对接）、臂 B live 驱动、金子集录制执行、SG §9 数值冻结（等 census/probe 回填）。

- **W5 Phase-1 实跑收口（7/15，用户提交 6f41c85；判决 = w5_specgate_design §9.1）**：census 9,988 samples/**102,194 事件**（K1 PASS）、确认基率 0.3961、gap p50 仅 0.3s（负类主体=微停顿）、zh/en p75 = 2.5/0.8s；**F_text 被 ASR 时滞排除（52.5ms>50ms 冻结门，差 2.5ms，不弯折）**；probe OOF AUC **0.6425**、prec@rec85 **0.4228**（基率+2.7pt）⇒ **K2 FAIL，SG v0 取消**（零训练、FDB 零接触）。负结果定性：**"本次静默会持续多久"纯时序不可判**；与 w4v3 内容探针（S 0.984）合成"**finality 信号是内容承载的，被 ASR 时滞墙挡在部署时点之外**"——双分离主张收窄为离线内容口径。**后继候选 SG v1 = ASR 同步门控**（地板余量算术：0.64−infer p50 0.561=**79ms ≥ lag 52.5ms**，p50 结构性保地板；新预注册 + 同门 K2'，ASR 缓存仓外落盘；K2' 再败则 SG 双探针负结果永久收口）——**是否立项待用户裁**。RB v2 正式构建：**config_hash 586da9e6a8c4**、1,000 episodes（dev/test 103/897）、修订占比 0.673、**pause_prior 已用（n=92,206）**、金子集清单 144 项。回归：155 passed、屏障探针 VERDICT MET/21-21/0 cache miss（引擎挂点零回归实证）；census 修复英文样本 lang 误标并全链重跑（K2 不变），census 加 --workers 并发。**下一步排序：① RB 臂 A runner + 臂 B live 驱动（关键路径，替我方全部评测面）② 用户 TTS 接线 + dev 音频听检 ③ SG v1 立项裁定 ④ 金子集录制排期。**

- **SG 线终局 + TTS 接线批（7/16）**：**SG v1 K2' FAIL ⇒ SG 线按冻结规则永久收口**（用户实跑 5389fc7：F_time+F_text_asr 15 维，OOF AUC **0.6646**、prec@rec85 **0.4419**<0.50；未训练/未生成 stophead/未跑 FDB/未接引擎）。判读（specgate §12.5）：ASR 内容仅加边际信号（AUC +2.2pt）；三段论定型——**意图稳定性不可学（W4 五代）/ finality 受控离线可分（w4v3 0.984）/ 部署时点时序与 ASR 内容双轨均不可达经济工作点（SG 双探针）** ⇒ 67.4% 废单率是结构性代价（论文注脚：content-blind 抢跑的定价）；方法学示范=两次预注册两次探针即杀、零训练消耗。**TTS 环境事实**：Qwen3-TTS 部署于 `/root/autodl-tmp/Qwen3TTS`（uv .venv，vllm 0.24.0+vllm-omni，bf16，服务 :8091，直接合成 `\.venv/bin/python scripts/synthesize.py --text .. --voice Vivian --language Chinese --output ..`；已验声 zh=Vivian/en=Ryan）。**QwenTTSBackend 已接线**（子进程调 synthesize.py、缓存仓外 /root/autodl-tmp/rb_tts_cache〔env RB_TTS_CACHE〕、自动重采样 16k、失败硬报错；音色映射 `exp/rb/tts_voices.json`〔cv01..cv09→预设名，**待用户提供 9 名单**，缺省回退 Vivian/Ryan=音色多样性受损须补〕）。**rb_run 增强**：臂 B live 音频（`--tts qwen|stub`：LiveAudio 实时合成+混音+真时长回填，decider 听增长前缀）；**臂 B 语义修正：at_after_eou 件是臂 A 名义投影，臂 B 计划中剔除、只走反应式事件**（臂 B oracle 基线数字将变，判分器未冻结属合法窗口）。**RB oracle 健全性（用户跑）**：A n=59 exact 0.7119/U 0.5518；B n=44 exact 0.5682/U 0.4503；双臂 wrong_commits=0、双跑逐字节。**判读要点：oracle 是窗口化 oracle（受同 δ=1.5 约束），其失分=机制天花板而非 harness bug——但须核对 by_layer：L2/L3/L4/L10 应≈满分，失分应集中 L5/L6/L8；若 L2/L4 不满分=harness bug，判分器冻结前必修。**

- **W5 冻结回归 + SG Phase-1 + RB 构建实跑（7/16；由 `6378243` 开始）**：第 0 批最终回归 **155 passed / 4 既有 warnings / 0 FAIL**，屏障探针 **VERDICT MET、21/21 确定性字段逐位、0 cache miss**。为利用 208 CPU，`w5sg_census.py` 新增默认关闭的样本级 `--workers` 多进程路径；64 样本串行/8 进程三产物逐字节同哈希，全量用 96 workers。数据质量审计发现并修复 loader 字段 `language` 被误读成 `lang`（旧写法会把英文全标 zh），新增 pytest 回归；修复只改事件语言标签/分语言报告，K1/K2 特征标签不变。最终 census：**9,988 samples / 102,194 events，K1 PASS**；事件 zh/en=`63,581/38,613`，确认基率 **0.3961**，gap p10/25/50/75/90：zh=`.1/.2/.3/2.5/2.8s`、en=`.1/.2/.3/.8/2.7s`，`gap<.64` 比例 `.6693`；ASR 投影 p50 `.0525s > .05s` ⇒ **F_text 不准入**。probe：OOF AUC **0.6425**，recall `.8501` 时 precision **0.4228 < 0.5**（θ=`0.3956668483`）⇒ **K2 FAIL**，故按预注册不训练、不跑 FDB，等待用户写 `docs/w5_specgate_design.md` §9 数值冻结。RB 链路：selftest **12/12**；pause prior `n=92,206`；`rb_v2.1` **1000 episodes / dev103 / test897 / revision .673**，config `586da9e6a8c4`、ids `f70727a59d9f`、content `6ec5658fcf2a`，独立重建逐文件相同；golden manifest **144**。正式产物：`exp/w5sg/{events.jsonl,census_report.json,pause_prior.json,probe_report.json}`、`exp/rb/build_v2/`、`exp/rb/golden/`。

- **D4–D6 代码/文书批已交付（7/07，单线程）**，全部机制藏默认关 flag 后，冻结路径逐位不动（37/37 + 新 89/89 单测 + 探针 VERDICT MET / 0 cache miss 复验）。清单与 GPU 出数命令：**`docs/w3_d456_handoff.md`**。
  - **现实档**：`src/latency_realistic.py`（κ 类对数正态，标定+预注册 `docs/latency_calibration.md`；per-instance sha256 播种=本地实现的 per-instance RNG；blocking 串行/TACT DAG 并行完成锚）。**纯记账层 ⇒ 决策缓存 100% 复用，δ 网格@现实档≈零 GPU**；`scripts/w3_realistic_rescore.py` 可对既有档案零 GPU 重打分。**预注册对表已出**（sblock vs tact 100 夹）：P-2 保费=**1.500 精确命中**（=δ*，档位无关定律实证）；P-3 链式条件式 31/33 恰过门（2 MISS 系两臂调用集不同）；P-4 齐 93/100（7=say 空 fallback）；P-1 写类 **0.237 ✓** 强达标 / 全量 **0.592 ✗**（>0.50，read_lookup 主导场景压比值——收益随工具时长放大的主张以**写类分层**形态成立，如实报）。
  - **状态轨双报 + 三分类**：`normalize_entity.py`（norm-v1 封闭规则集，无别名扩展）+ `w2r_state_track.py` 双报改造 + `w3_state_classify.py` 台账。**已出数**：TACT 57→**76**/100、sblock 57→75、blocking 57→74（P2' 差分制维持 TACT≥blocking；FT=0 无倒挂）。失败分类：format 24 主体 / missing_call TACT 7 < blocking 11 / **蓝图#8 试点 TRIGGERED**（asr_mishear 两臂≥3，立项待裁断）。
  - **DAG+补偿**：`src/tact_dag.py`（声明模板+值流证据边；patch 命中依赖字段→pending 下游重参数化/stale+窗重启、committed 下游→补偿计划；κ-忠实注册表 reverse 模板+幂等键 at-most-once）。`--dag on` / engine_cfg `dag:true`。
  - **speculative_dispatch**：core 算术（t_dec=seg_end+max(0.64,infer)）+ TactEngine 真实装（vad end 派发、EoU 确认前 inert、语音恢复作废、finalize 释放 guard——四组单测含屏障互作）。`--speculative on` / engine_cfg `speculative_dispatch:true`。
  - **分句 TTS + floor v0**：`tts_sentence.py`+`floor_policy.py`+TactEngine `tts_split`/`floor_holding`（TtsSentDone 事件流、first_sentence 完成锚仪器、barge-in 丢弃未播句；narration 无条件 yield，confirmation+commit 临近 finish_clause，粒度=句）。
  - **prompt v3 五靶**：`PROMPT_V3_ADDENDUM`（规则 10–14：后提交瘫痪/宣告即撤销/patch 对字段/自我打断替换/实体规范形态）`--prompt v3` 或 engine_cfg `prompt:"v3"` 显式装配；**30 调优子集已预注册** `exp/w3/tuning30.json`（21 rollback + 9 证据夹），协议 `docs/prompt_v3_five_targets.md`。驱动器新增 `--ids-file`。
  - **R18 缓存键审计闭合**：sha256(messages) 无 δ/档位/barrier 文本耦合；快照差异均属轨迹性合法臂差。
  - **剩余 GPU 项**（交用户，命令在 handoff §3）：30 子集现实档冒烟、现实档全量+δ 网格 A 档、投机 A/B+HumDial 门、DAG 链式冒烟、prompt v3 30 子集→冻结→全量+双档网格、分句 TTS live 增益（E5）。

- **W5 SG v1 + RB v2.1.1 实跑归档（7/16；基线 b05e963）**：w5sg_asr_features.py 补齐与 backend 同款的 SenseVoice model.int8.onnx 优先/model.onnx fallback，并新增默认关闭的样本级 --workers 多进程路径与原子仓外缓存；6 样本/90 事件串行 vs 4 worker 输出逐字节同哈希，全量 64 worker。SG v1 得 **9,988 samples / 102,194 events / 15 维**；与 v0 的标签、gap、语言、group、前 6 维逐行 **0 mismatch**，schema/有限数/前缀单调违规均 0；events SHA256 79b8c3ed84a12f7766c10cd11ce49d99ab1c55ceda74d42e26f693427cb6f52d；仓外 ASR cache **102,194 全可解析 / 26 空 / 0 坏**。OOF probe：AUC **0.6646**、recall .8500 时 precision **0.4419 < K2' .50**（信息性 θ=0.3864257762969059，dispatch 77,872 / positives 40,482）⇒ **K2' FAIL；SG 线按 §12 永久收口，无 v2、零训练/stophead/引擎接线/FDB**。RB 重建 manifest：**rb_v2.1.1 / config b30499ad9de7 / content 9a735aecc07d**，1000 episodes（dev103/test897，revision .673，ids f70727a59d9f），1000/1000 episode hash 新值、旧 586... 残留 0，golden **144**，无音频（TTS 本轮不需要）。runner selftest **10/10**；oracle text dev A：n59/exact .7119/U .5518，B：n44/exact .5682/U .4503，两臂 wrong/unrepaired=0；低分集中在时序受限的 L5/L6/L8（oracle 只替代决策内容，δ/屏障/可逆性压力保留，不要求全金），报告+103 逐夹双跑逐字节一致。最终回归 **156 passed / 4 既有 warnings / 0 FAIL**；屏障探针 **VERDICT MET / 21-21 / 0 cache miss**。正式新增产物：exp/w5sg/{events_v1.jsonl,events_v1_meta.json,probe_report_v1.json}、exp/rb/build_v2/{manifest.json,rb_report_rbdev_oracle.json,rb_report_rbdev_oracle_b.json,results/{rbdev_oracle,rbdev_oracle_b}/} 与更新后的 exp/rb/golden/。

- **RB 臂 A 音频 + B2 冻结前核对（7/16）**：用用户给定 9 音色映射运行 rb_build --audio qwen exit 0；600/600 臂 A WAV 均为 mono PCM16/16k，missing/extra=0，cue 数/measured gap/边界/6s tail 全量一致，音频 340MiB 已由 .gitignore 排除；仓外 /root/autodl-tmp/rb_tts_cache 共 **880/880** 个 24k 原始 WAV、187MiB、0 坏/0 tmp，9 音色实际片段覆盖 cv01..09=98/132/142/135/124/150/168/124/127。manifest 仍为 config b30499ad9de7 / rb_v2.1.1 / n1000 / dev103-test897，audio=qwen，content hash **8a2ae43c7e10**。臂 B 语义修正后 provider rbdev_oracle_b2：n44、exact/state **0.7045**、U .5638、wrong/unrepaired=0；L10 1.0 / L4 .8333 / L5 .1818 / L6 0 / L8 .9412 / L9 1.0；相对旧 B .5682 的主增益来自 L8 .5882→.9412。B2 报告 SHA256 6a1d05415d596f8b8978ca0663ff57aaffd5b2188a695a0cb6213e3d029764b3，双跑报告+44 逐夹逐字节一致；runner selftest 10/10、全套 **156 passed / 4 既有 warnings**。**冻结核对未通过，严禁此时哈希入册或起决策 vLLM**：唯一 B-L4 miss B_L4_0005 的 amount 同时进入 get_fx_quote 与 transfer_funds，但 OracleDecider.slot_arg 是 slot→单一 (step,arg)，循环覆盖后只 patch transfer，前者保留旧值；A_L3_0061/A_L4_0009 同源。下一步须先把 oracle 改为 slot→[(step,arg),...] 并重跑 A/B oracle，再按 by_layer 决定是否冻结 scorer；这属于 oracle harness 修复，不改 scorer。

- **RB 冻结后 oracle 确认 + 首次音频 LLM dev（7/16；基线 `1998c14`）**：冻结三文件哈希在本机复核全匹配，runner selftest **12/12**。oracle A3 `n=59/exact=.7458/U=.5761`，L1/L2/L3/L4/L7/L9/L10 全 1.0、天花板 L5=.2143/L6=.25/L8=.6667；oracle B3 `n=44/exact=.7500/U=.5974`，L4/L8/L9/L10 全 1.0、天花板 L5=.1818/L6=0；两臂 wrong/unrepaired=0，与 `scorer_freeze.json` 机器断言逐项一致。报告 SHA256 A3=`1ff2273b4e278cd4db3f0b511336eeff0c811d6fbbedd3089d31457b60dcaa24`、B3=`756f9c492162cf226f10d06140a912062d8d43032798a7ebeb453751ee453e05`。随后按顺序启动 `qwen3_omni_audio_single_gpu.yaml`（max_num_seqs=1）+ :10004 代理，所有 177 个 POST 均 200：TACT `rbdev_tact_d150` 首跑 cache **0/118 hit/miss**，`n=59/exact=.0508/U=.0383/first50=1.640/done50=3.809`，by-layer exact L1/L2/L3/L4/L5/L6/L10=0、L7=.25、L8=.3333、L9=.1429；blocking `rbdev_sblock` 首跑 cache **0/59**，`exact=.0339/U=.0189/first50=1.989/done50=2.334`，仅 L8=.3333/L9=.1429，其余层 0。配对仅 TACT gain `A_L7_0012`、0 loss；**L4/L5 双臂均 0，预期的事务窗层分化未出现**。这不是解析/接线伪失败：TACT/阻塞原始输出分别 **118/118、59/59 严格 JSON**，0 repair/0 salvage/0 parse error、invalid launch fn=0；TACT 106 个 say event/58 夹（其中 raw blank+launch 的 ack fallback 37 次），blocking 22/22；FC 关闭故 marker commits=0，双臂 wrong/repaired/unrepaired=0、comp_cost=0。服务关闭后全缓存离线复放为 **118/0、59/0 hit/miss**，两报告、两缓存和全部逐夹聚合哈希逐字节不变；报告 SHA256 TACT=`da961598d0c8447120fb11ff3c70b5d165a1a7ef5f33b9fcfd667050a2d04ff4`、blocking=`850b61bbec32cf85c8e52a9eaa07e15223214dcc8d905bbc98632914ce5c3fdc`。vLLM/代理已停、GPU 释放；**不得自行进入 test 897**，先把两份全文交用户裁定并等待模型动物园单发协议。

- **RB v2.2 重建 + 第二次音频 LLM dev（7/16；基线 `10566e0`；完整收据 `exp/rb/build_v2/rb_dev22_validity_receipt.json`）**：按 `rb_v2.2/config=9f8f4ae9edc8` 确定性重建 1000 夹（dev103/test897，content `17cceb7303f3`）并重跑 Qwen TTS；臂 A **600/600** WAV、mono PCM16/16k、cue/gap/边界/≥6s tail 全过，357,561,546 bytes；仓外 cache 1,409 WAV/324,776,236 bytes，当前构建 862 unique key 全命中，0 坏/0 tmp。oracle A4 **.7627**（L1/L2/L3/L4/L7/L8/L9/L10 全 1；L5 .2857/L6 0）与 B4 **.7500**（L4/L8/L9/L10 全 1；L5 .1818/L6 0）逐项命中冻结 v2。真实音频 dev 串行结果：TACT `rbdev22_tact_d150` **n59/exact=.2542/U=.1889/first50=1.640/done50=3.793**，by-layer L1 .3333/L2 1/L3 .2308/**L4 0**/L5 .0714/L6 0/L7 .75/L8 .3333/L9 .5714/L10 .5；blocking `rbdev22_sblock` **exact=.1695/U=.1073/first50=2.116/done50=2.235**，L1 .3333/L2 1/**L3/L4/L5/L6 全 0**/L7 .5/L8 .3333/L9 .5714/L10 .5。配对 TACT-only 5、blocking-only 0、both-pass 10、both-fail 44。解析口径须勘正：最终不可解析均 0，但原始并非全严格 JSON——TACT 118 逻辑决策/142 raw：94 首试严格、24 首试坏，repair 严格救回1、salvage 23；blocking 59/77 raw：41 严格、18 坏，repair1、salvage17。逐夹结果分类（互斥末态）：TACT exact15/tool-inventory23/required-arg-invalid4/unresolved-ref9/value8；blocking exact10/tool-inventory41/required-arg-invalid2/unresolved-ref1/value5。缓存首跑 TACT 2/140、blocking 1/76 hit/miss；停服后复放 **142/0、77/0**，报告/缓存/逐夹目录 SHA256 逐字节不变。
  - **仍属构建有效性 FAIL，非可发表模型电平**：① blocking 在 `apply_decision_ops(immediate=True)` 已提交后才填 `batch_of`，批内 `$RESULT_0` 提交时仍为字面量（`A_L4_0002` 实证）；② `src/tact_dag.py::DAG_TEMPLATES` 只有 FDB 工具对，RB 三类链一个模板都没有，故 runner 虽 `--dag on` 也产生不了 RB edge；现有 selftest 用 oracle 预铸结果 ID，未覆盖真实 RB 依赖传播；③ catalog 未声明 city/slot 的 canonical 语言，模型输出成都→Chengdu 等被 exact 杀（配对成功调用 TACT 9 处/blocking 5 处）；④ prompt v3.1 规则14明确去前置冠词，gold 却保留 `the landlord/property office`，两臂各 2 个 L9 被构造性杀。日期没有 format-only 残差（TACT 两处 May 8/June 1 是语义旧值）；seat_class 仅 TACT 2 处 `first class→first`。收据附 3 个 L4 cache key+原始输出（含 malformed→salvage、错 patch、blocking 批内 ref）。最终回归 **156 passed / 4 既有 warnings / 0 FAIL**，屏障探针 **VERDICT MET / 21-21 / 0 cache miss**，build/runner selftest 各 **12/12**。**裁决：不得跑 test 897；先修上述 runner/DAG/catalog-gold，补真实链回归，按纪律重冻并重跑双臂 dev。**

- **RB rb_v2.2.1 重建 + 第三次音频 LLM dev（7/16；源提交 `8312904`；完整机器收据 `exp/rb/build_v2/rb_dev3_validity_receipt.json`）**：按 `config=265c7cd8f485` 重建 1000 夹（dev103/test897，content `216b79de9cee`，ids `f70727a59d9f`），Qwen 增量合成 exit 0；臂 A **600/600** WAV 全为 mono PCM16/16k，1200 cues、tail min 5.999937s，当前 870 个仓外 TTS key 全命中、0 missing/0 tmp。冻结 v3 三文件 sha256 全匹配；oracle A5 **.7966**（L1/L2/L3/L4/L7/L8/L9/L10 全 1，L5 .4286/L6 0）与 B5 **.7273**（L4/L8/L9/L10 全 1，L5 .0909/L6 0）逐项位齐。真实音频 dev 串行：TACT `rbdev3_tact_d150` **n59/exact=.2034/state_norm=.2203/U=.1459/first50=1.640/done50=3.929**，by-layer L1 .3333/L2 0/L3 **.0769**/L4 **.25**/L5 0/L6 0/L7 .25/L8 .3333/L9 .8571/L10 0；blocking `rbdev3_sblock` **exact=.1356/state_norm=.1695/U=.0659/first50=2.127/done50=2.242**，L1 .3333/L2–L7 全 0/L8 .3333/L9 .7143/L10 .5。故窗口敏感层方向正确：**L3 .0769>0、L4 .25>0**。首跑 cache TACT 0/157、blocking 0/85 hit/miss；停服断网复放 **157/0、85/0**，两报告 SHA 前后逐字节一致。解析末态失败均 0，但 salvage 是系统属性：TACT 118 logical/157 raw，首试严格79、坏39、repair救4、salvage35；blocking 59/85，严格33、坏26、repair2、salvage24。互斥失败分类：TACT exact12/rejected18/inventory17/unresolved5/value7；blocking exact8/rejected8/inventory38/unresolved1/value4（重叠 inventory 35/46，仍为最大死因）。
  - **dev 构建有效性门 PASS**：合法 blocking 同批 prior ref 在 `A_L6_0004` 实跑把 `$RESULT_0.products[0].product_id` 解成 `SE9046dd`，0 字面残留；blocking 仅余 3 个字面 ref 均为模型自/未来引用，L4 unresolved=0。TACT 实跑产生 **28 `dag_edge` / 5 `dag_stale`**（22/5 夹），五次均对 RB 子链重启 pending 窗；oracle A-L5 .2857→.4286 是同向外证。全库 gold 前置 `the`=0、裸 `first`=0、完整 `first class`=55；实际两臂裸 first/前置 the 也均 0。所有整层零均由缓存原始输出的漏/多工具、缺参/未知工具、旧值、malformed→salvage、或非法自/未来引用穷尽，未再发现 bench 侧结构性死因。非门控残差如实留档：共同 `A_L2_0007` 把 catalog 例值抄成 `May 3 / 5月3日`；payee Title Case 造成 exact-vs-normalized 差 1/2 夹；TACT 仍有 5 处违反已声明 verbatim 的中→英值。
  - 拉取后 `rb_build --selftest` 曾因旁观者值 `一` 子串误撞地址 `学院路一号` 假 FAIL；仅把断言改成 gold 参数值级精确比较，**不改生成内容/config/scorer/freeze**，selftest 恢复 12/12。最终回归 **156 passed / 4 既有 warnings / 0 FAIL**，runner selftest **14/14**，屏障探针 **VERDICT MET / 21-21 / 0 cache miss**；golden manifest 144。**本轮从未运行 test 897；dev 窗口可按协议关闭，继续等待用户给出的模型动物园正式单发协议。**

- **RB test-897 第一批正式单发收口（7/16；协议/源提交 `152a5f6`；机器收据 `exp/rb/build_v2/rb_test_receipt.json`）**：冻结构建仍为 `rb_v2.2.1/config=265c7cd8f485`，判分器冻结 v3 每次 test 前自动验哈希。15 个 provider 严格按协议顺序各跑一次，**0 provider 重跑**；13 个 LLM provider 共 4,559 次真实 cache miss，全部由本地 `Qwen3-Omni-30B-A3B-Instruct` 音频栈（:10003→:10004、`max_num_seqs=1`）HTTP 200 返回。臂 B 冻结命令缺 runner 必需的 `--tts`，仅机械补 `--tts qwen`；正式跑前仓外预热 537 unique 段（352 hit/185 synth），正式运行 0 合成。主表：A-TACT **n541 exact .2662/state_norm .2828/U .1996/first50 1.640/done50 3.889**，A-blocking **.2181/.2440/.1410/2.176/2.302**；B-TACT **n356 .2781/.3146/.2078/1.640/4.050**，B-blocking **.1910/.2303/.1342/1.967/2.268**；oracle A/B **.7763/.7865**。全 provider wrong_commits=0、unrepaired=0、comp_cost=0。
  - **冻结假设对账**：屏障 on→off exact **.2662→.1885**，配对 on-only/off-only=44/2；L4 **.0938→0**（6/0），强支持。δ exact @0/.6/1/1.5/2s = **.1922/.1922/.2366/.2662/.2865** 单调，first50 恒 1.640、done50 2.150→4.503。DAG on/off **.2662/.2606**，但预注册 L5、L7 均 0 翻转（L7 生成的是独立 multi-call、实际 0 edge），故**预注册 L5/L7 劣化方向未获支持**；只读 0-miss trace 重建确认 219 edge/29 stale（28 夹），3 个 on-only 成功落 L3×1/L4×2。FC 三档 exact 全同 .2662；全量 first50 v1/filler/silent=3.352/1.640/3.818，L9 各档 exact=.8308 且 first/done 同 7.550/7.708。
  - **L10 参数流双率（非 whole-episode exact 代理）**：A-TACT 良性本人修订进入已执行工具参数 **8/13=.6154**、第三方命令拒绝 **7/14=.5000**；A-blocking **1/13=.0769、13/14=.9286**，定量钉住开放异议窗的效用-安全交换。B-TACT 为 4/12=.3333、2/13=.1538；B-blocking 5/12=.4167、9/13=.6923。H-B1 的 pre-call/in-flight 条件没有跨状态匹配 pair id，预注册 McNemar **不可合法计算**；描述率还受 action 混杂，收据明确标 `not_testable_as_paired_McNemar`，不得拿非配对替代结果。
  - 全部在线服务随后停止/GPU 释放；15/15 离线复放（LLM cache **0 miss**、oracle 重算）报告、13 缓存、15 逐夹目录 SHA256 **逐字节一致**。唯一 pre-test 基础设施异常是首个 detached TTS 进程随 shell 退出，发生在任何 provider 之前；持久前台重启后预热完成，不构成单发重跑。**test 窗口已用完：generator/scorer/runner 后续任何改动仍必须公开勘误+版本递增，不得改后重跑本批 test。**

- **RB test-897 第一批判定（7/16；判读全文+主表+配对表 = `docs/rb_test_protocol.md` §五，协议正文未动）**：镜像容器对 15 provider × 8,656 归档行独立重算 headline/by-layer/配对/δ 曲线/FC tier/臂 B 分层/oracle gate，**全部与 report+收据逐位一致**（p50=runner 上中位数口径；L10 参数流与 DAG trace 计数依 0-miss 逐字节复放采信，音频不入仓无法容器内重放）。**五假设判定**：① 屏障**支持**——L4 配对 6/0（p=.031）、L3 9/0（p=.0039）、全体 44/2（McNemar 精确 p=3.1e-11），且 nobar .1885 < blocking .2181 = **无屏障的窗口是净负资产**（off 连带 L7 .386→.068、L8 .298→.123：窗口价值以 decision-atomic commit 为前提）；② DAG **未获支持**——预注册 L5/L7 逐位 0 翻转（L7 生成为独立 multi-call 0 边可断、L5 失败质量在 DAG 裕度之下：LLM .0122/oracle 上界 .122），净效应 +3/0 落 L3×1/L4×2（stale 重启窗救竞态修订），机制活性有据但法庭选错层；③ H-B1 **不可合法计算**（无跨状态配对键=生成器仪器缺口；描述率反向：失败质量集中 pre-call 地板 .0065，不得引用原表述），可进结论句的替代事实 = **armB L8 配对 34/0（n=103，p=1.2e-10）**：in-flight 生命周期是分化主战场且 armA 脚本化投影测不出（armA L8 反向 6/11 n.s.）——臂 B 存在理由的实证；④ H-FC1 **不可判**（三重死仪器：hedge/commit 在 launch-time-only 钩子下结构不可达〔v1 实选 silence 377/filler 228/progress 64，L9 全 progress=策略按设计运转〕、承诺-修复轨 0 事件、L9 first 锚 62/65 回退 done）/H-FC2 **平凡真**（outcome 轨构造等式）；**post-hoc 双工面**（非预注册）：say 起点 talk-over 率 filler 27.8% / v1 14.1% / silent 0%（blocking 0%）× first50 1.640/3.352/3.818 = 单调前沿、v1 内点无支配，Ultravox 恒填充 47.9% 撞用户现象形态在 RB 复现；⑤ L10 双率**描述级**（n=12–14<30 按冻结纪律不进结论句）：TACT 拒绝率两臂均大幅低于 blocking（.500/.154 vs .929/.692）=窗口安全代价跨臂一致，良性收益仅 armA 兑现（.615 vs .077；armB .333 vs .417 双轴不占优）。**附带**：L1/L2 通过集两系统逐位同=harness 对齐锚；δ=0 (.1922)≤blocking=eager 惩罚同构复现；完成保费≈δ、首响恒 1.640 与 δ 解耦=FDB 两定律跨基准复现；主臂差 +4.8pt(A)/+8.7pt(B) 配对 41/15、36/5 均过精确检验=非刀口差；三条死仪器（承诺-修复/补偿费/L9-FC first 锚）如实入册。**下一步 = 第二批 learned-head RB 适配层（stophead REQUIRED_ARGS/κ 面向 FDB 工具 → RB 等价物）+ selftest，交付后按 §一单发 v2/C0/C1 π 点。**

- **RB 第二批适配层交付 + §六冻结（7/16）**：`rb/learned.py`（RB_REQUIRED_ARGS ← 冻结 registry `TOOLS[fn]["required"]` 单源、`install_stophead_rb()` 零重叠断言注入、`load_head()` twostage 校验；κ 走 install_rb 既有 REVERSIBILITY 注入；RB 四域名恰与 FDB DOMAINS 同名）+ `scripts/rb_run.py` **runner v1.1 增量勘误**（`--delta-policy learned:v2 / --stophead-model / --finality-cache`，默认 fixed；finality = 冻结 FINALITY_PROMPT 音频尾 8s Omni 判读、独立缓存、不进决策消息 ⇒ 决策缓存键口径不变；learned 决策行加 op_windows/finality 审计键，冻结路径零新键；guards：learned⇒tact、δ>0、llm⇒audio；report 附 config.stophead+learned_windows.protect_rate）。**默认路径不动的证明 = 扩展 runner 重放两个 oracle test provider，897/897 行逐字节等于归档 rbtest_oracle_a/b、报告除 provider 名外相等**；runner selftest **14→21**（protect-all ≡ 固定 δ1.5 结构等式〔scored 字段全等+窗口全 1.5〕、commit-now 丢 L4 救援且 done 缩短、双跑逐位、冻结路径无审计键、REQUIRED_ARGS/κ 全解析、冻结 v2 权重 risk(unfinished)>risk(final)）；rb_build 12/12、tests/test_w5 13/13（pytest 缺容器、手动注入 tmp_path）全绿。CLI 冒烟：C0@π.10（θ=.055792）oracle dev-A text-fallback finality 下 protect 166/166、exact .7966 = 归档 A5 gate 值精确复现（protect-all≡fixed 的端到端外证）。**§六已冻结**（batch-2 test 观测前写定）：17 provider 单发清单（v2 + C0 π 升序 8 + C1 π 升序 8；缓存链式种子、finality 缓存臂 A 共用一份）、只许前缀截断不许挑点、臂 B 学习头不在本批；**判读只有三条**：H-LH1a protect_rate 随 π 机械单调（违反=适配 bug）/H-LH1b exact 弱单调趋向 .2662/H-LH2 **主判读 = 无任何学习头臂对固定臂配对净优势（p<.05）——预测不发生，发生则 W4 "G2 未立"不迁移属重大更新**/H-LH3 逐 π 配点 C1 protect ≥ C0；已声明风险 = RB 修订基率 .673 远超 π 支撑 [.02,.30]，标度错配为预期内、不构成重训理由。**待用户按 §六单发；判读写 §七。**

- **RB test-897 第二批学习头正式单发收口（7/16；源提交 `3e07ea2`；机器收据 `exp/rb/build_v2/rb_test_receipt_batch2.json`）**：冻结构建/判分器不变（`rb_v2.2.1/config=265c7cd8f485`、freeze v3），runner selftest **21/21**。17 provider 严格按 v2→C0 π 升序→C1 π 升序各跑一次，**0 provider 重跑、无前缀截断**；首个决策 cache 种自 batch-1 固定主臂，之后逐 provider 链式接种。在线共 **427 decision miss + 934 finality miss = 1,361 次**本地 `Qwen3-Omni-30B-A3B-Instruct` 音频请求，全部 HTTP 200；共享 finality cache 最终 934 entries（首臂 106/934 hit/miss，后 16 臂各 1040/0）。v2 头 **exact .2662 / protect .9939**，与固定臂通过集 541/541 逐位相同。π={.02,.04,.06,.08,.10,.15,.20,.30} 前沿：C0 protect **.1054/.2344/.2559/.2690/.2690/.7764/1/1**，exact **.1922/.1941/.1941/.1941/.1941/.2662/.2662/.2662**；C1 protect **.1351/.1895/.2495/.2690/.4944/1/1/1**，exact **.1922/.1922/.1941/.1941/.2421/.2662/.2662/.2662**。
  - **冻结判读的机器读数（正式措辞留给 §七）**：H-LH1a 两族 protect 弱单调、H-LH1b 两族 exact 弱单调，均无适配器故障；H-LH2 预测兑现——**没有任何学习头对固定臂取得显著配对净优势**，低/中 π 的 10 个 provider 反而显著更差（典型 learned-only/fixed-only=6/45–46；C1 π=.10 为 4/17, p=.0072），余 7 个 provider 通过集逐位相同；H-LH3 **未逐点成立**，8 点中 6 点含 tie 满足，π=.04/.06 为 C1<C0。该标度塌缩与预声明风险一致：RB episode 修订率 .673 远超 π 网格支撑，不允许据此回流重训/挑点。
  - 在线后 Omni/代理停服、端口关闭、GPU 释放；随后 17/17 离线复放，所有 provider **decision 0 miss + finality 0 miss**，17 报告、17 决策缓存、共享 finality cache、17×541=**9,197** 份逐夹 JSON 的 SHA256 复放前后全同。正式异常 **0**；仅记录两个无影响的非正式观察：首跑前代理 `GET /v1/models` 因只暴露 POST 路由返回 404，以及停服时 resource_tracker 清理警告（端口/GPU 均确认释放）。**本批 test 窗口已用完；下一步只由判读层写 §七，不再运行或改后重跑。**

- **RB test-897 第二批判定（7/16；判读全文+π 前沿表+结构读数 = `docs/rb_test_protocol.md` §七）**：镜像容器对 17 provider 归档行独立重算 headline/by-layer/protect（由逐夹 op_windows）/对固定臂配对/精确二项 p，**全部与收据逐位一致**。**四判读裁决**：H-LH1a/1b 双 PASS（两族 protect 与 exact 均弱单调；protect=1.0 的 5 点通过集与固定臂逐位同 = protect-all≡fixed 端到端兑现）；**H-LH2 主判读预测兑现——0/17 显著优、10 显著劣、7 逐位同 ⇒ W4"G2 未立"零训练迁移到修订密度 8 倍的第二基准**；H-LH3 未获逐点支持（6/8 含平，π=.04/.06 违例：θ_C1<θ_C0 但 C1 protect 更低 = 风险场权重差盖过阈值排序，族内单调双 PASS 证明非适配层缺陷）。**结构读数（§7.3）**：① 价值捕获账本——protect .19–.27 只捕获 +1/40（2.5%）、C1@π.10 .494 捕获 +27/40、≥.776 收全 = 价值集中窄 risk 带（FDB 质量悬崖第三现场）；② **L7 反窗口夹（机制已当日勘误，勘误文 = §7.3）**——低/中 π 对固定臂的 learned-only 全落 L7（6/6、4/4）；初版"cancel 吞 pending"猜测被逐夹轨迹证伪，真机制 = **修订目标归属错误**：修订指向第二步槽位（country/item_id），决策器 patch 进仍开窗的第一步 search（6/6 夹有 `patch op_id=2` 直接证据；blocking 单发合并同样错）——**开着的窗口是歧义修订的错误吸附面**，commit-now 移除吸附面故"更快=更对"；正确结论 = 需要修订目标归属的准入控制（→ W6）；③ **效率边际 = 学习头唯一正贡献（描述性）**：C0@π=.15 通过集与固定逐位同、22.4% op 立即提交、均值保费 4.120 vs 4.560（−0.44s）、**U .2029 > .1996 = 预注册效用上严格弱帕累托改进**，与 FDB C0@π.30 无损支配 fixed **跨基准复现**（无损点 π 基准相关 .30↔.15，须成对引用）；④ v2 同 θ=.03 在 FDB/RB 读 protect .84/.9939 = 风险绝对标度不跨域的第二证据；first50 恒 1.640 = 首响解耦第三次复现。**学习线终局（§7.4）**：意图稳定性不可学（W4 五代）+ finality 仅离线可分（w4v3 0.984）+ 部署时点双探针不可达（SG）+ **冻结头排序在第二基准 0 胜（本批）** = 双基准否定链闭合；δ1.5+屏障作为方法核心的选择获得完整否定侧证据。**test-897 全部窗口（15+17=32 providers）已耗尽，RB v2.2.1 上不再有合法 test 运行（新系统版本单发不受限）。**

- **W6 准入控制立项（7/16，用户裁定 in-paper；Phase-0 结构冻结 = `docs/w6_admission_design.md`）**：用户驳回"SV 门控/意图理解放 future work"——两者进本篇。立项证据链：① WHO 轴 = L10 双率（TACT 拒绝 .500/.154 vs blocking .929/.692，良性穿透 .615 要保住）= 蓝图 §4.6 SV 门控复活；② WHICH 轴 = L7 反窗口夹勘误后的真机制（修订目标归属错误，开窗 = 错误吸附面）；③ 结构面已核验：多声音 episode 全部且只在 L10（26/897）⇒ SV 门在 871 夹结构性不触发。机制 = 决策级准入：**SV 门**（触发段说话人验证失败 ⇒ 丢弃该决策全部 ops；SV-oracle 用构建 voice 标签 = 上界行 + SV-real 用 CAM++/ERes2Net 类嵌入器、enrollment=首段、τ 在 dev 冻结）+ **归属门**（RB prompt 增量规则：修订绑定值语义所属字段，指向未 launch 动作则带值 launch；dev 最多两轮，五靶纪律）。batch-3 = 6 providers（A：svoracle/svreal/attr/svattr；B：svreal/svattr）。预注册判读：P1 SV-real 对抗拒绝 ≥13/14 且良性不降且非 L10 误伤 ≤1；P2 armB 拒绝 ≥.692；P3 归属门收复 ≥4/6 且 L3/L4 15 个救援夹 0 损失且全量 ≥.2662；P4 组合近似相加。Kill：K1 dev 单声夹误拒 >2 ⇒ 只发 oracle 行；K2 两轮词条零和 ⇒ 封盘出负结果；K3 runner v1.2 平价复放不过 ⇒ 修完再进 dev。**同文件 §七归档基准合理性分解（回应"共同失败=任务不可能"）：382 共同失败夹中 270（70.7%）oracle 可过 = 内容层失败、真天花板仅 112（20.7%）= L5/L6 设计余量；可完成子集 TACT/blocking = .338/.264。下一步 = W6 Phase-1（嵌入器选型听检 + dev 迭代 + §6 数值冻结）→ batch-3 单发；金子集录制与四基准核对并行不变。**

- **外部评审核验 + RB v2.3.0 实现批（7/16；用户裁定：范围 = 评审 §五 items 1–5 + 归属门，W6 缩编为 SV 门单项记录、当前仍在 W5）**：评审全部可数主张**逐条实证成立**（核验表 = `rb_design.md` §15.1；两处数字修正：A_L8 实测 57 test 夹、B-L6 dev=2）——臂 B 自我重叠 37/356、B_L4 in-bin 5/54、修订双投放 141/141、committed 锚 0 触发（恒空切片+`revisions[0]` 错引互相掩盖）、臂 A 投影双计 HOLD（0/57 入执行窗）、10/28 零 gold 工具恰为全部 reverse 类、域=语言 100% 耦合、blocking 单决策 {1:246,0:3}、唯一句 296/121/417、dev→test 意图句逐字复现 83%。**已发布结论勘误 = `rb_test_protocol.md` §八**（零重跑）：臂 B L4/L5/L6 行作废（勘误表 `exp/rb/errata_b_rebin_v221.json`：in-bin 子集两臂 exact≈0）；"生命周期触发"收窄至 L8/L9/L10-B（armB L8 34/0 安全）；blocking 口径注记；"补偿考场"说法移除；域=语言入 limitations。**v2.3.0 实现**（config `3c36d0a9ae1b`、1000 夹 dev/test 89/911、域×语言解耦 117–136/格、确定性重建过）：item1 零成本五修〔lang 哈希采样、feed_sim 水位线+末段回灌（committed 锚全链路活）、事件文本按 content-kind 选修订、`at_after_eou` 真 EoU 相对（在飞事件按构造入窗）、重分箱勘误表〕；item2 臂 B 先合成后排期（臂 A 拼接器同构）+ `ARM_B_EVENT_ONLY` 单投放 + 每行 `armb_timing` 收据（overlaps 必须 0）；item3 事务考场〔`execute(t=…)` 执行期状态 + `abort()`（REV/COMP 执行中可中止零费、IRR 不可、abort 不计 C_κ）+ **reverse 工具净额一等公民**（命中在册 forward 即对消+COMP 计费，外部系统可被补偿轨计分）+ **L7 重定义 = 补偿考场**（gap 按构造过参考提交视界，gold=forward(new) 净额，补偿路计费 vs 长窗 patch 路免费 = def2 经济学；oracle 决策器新增补偿路）+ idem_key + occurrence 键控时延〕；item4 **L11 = TTS 打断改口层**（tts 锚 + 0.05–0.40s，屏障接力保证可救援）+ **L12 = 归属考场**（step-2-only 槽位约束 = v2.2.1 反窗口夹机制显式复刻；配额重排总量 600+400 不变）；item5 `scripts/rb_content_gen.py`（DeepSeek `deepseek-v4-flash` 非思考、离线一次性生成冻结内容库、验证器把关、bank 哈希入 config_hash、bank 缺席=模板回退）+ 五类不流利 + 播种扰动族（rate/gain/SNR，合成后应用、TTS 缓存键不变）+ 冻结承诺 judge（`COMMIT_JUDGE_PROMPT`+`make_llm_judge` = 承诺轨对外可用）；**归属门 = `--attr on` + `PROMPT_RB_ATTR`**（默认 off 逐字节不变，dev ≤2 轮后冻结）。在飞锚改"首个非 READ launch"（评审写 COMP/IRR；纳入 REV 的偏离理由已记 §15.2）。回归：runner selftest **34/34**（+13 v2.3 机制检查）、rb_build 12/12、W5 13/13、simulator 双路径 PASS。**v2.2.1 归档一字不动；v2.3 未跑任何 test，dev/test 纪律重新起算（scorer/sandbox/registry 已变 ⇒ v2.3 test 前另立冻结 v4）。下一步 = ① 用户跑 `rb_content_gen.py`（DEEPSEEK_API_KEY）+ bank 听检入库 ② v2.3 构建 + Qwen TTS + dev 有效性门（oracle 门层重验）③ attr 门 dev 迭代 ④ 冻结 v4 → 新 test 协议单发。**

- **RB v2.3 内容库 + 构建/dev 首轮收口（7/17；机器收据 `exp/rb/build_v23/rb_dev23_receipt.json`，详述 `rb_design.md` §16）**：从 `04ab6ff` 拉取后，`rb_content_gen.py` 对 52 类各单发一次 DeepSeek（共 52 API calls；312 requested / 246 raw / **244 accepted**；0 手改/补采/重试），冻结 bank SHA `309c7c0d0361…` 并以独立提交 `3ebc337` 入库。由于 bank SHA 是 config 输入，`3c36d0a9ae1b`（bank=none）自然升级为正式 **`e1a515c29b8a`**；1000 夹 A/B=600/400、dev/test=89/911、域×语言 104–143/格。`exp/rb/build_v23` Qwen3-TTS 正式构建 600/600 WAV（mono PCM16 16k、365MB、1200 cues），全 cache-hit 重建 manifest/episodes/audio 逐字节同；旧 build_v2 未动。运行中修三处适配缺口并加回归：canonical 数值自测、结构化 `l8_action` oracle cancel、state scorer 排除 reverse/abort 的 void 历史项；runner selftest **34/34**、build 12/12、W5 13/13。**oracle dev 门**：A exact/state .8704（门层 L1/2/3/4/7/8/9/10/12 全过；L7 补偿 reverse+relaunch 4/4；L12 3/3），B .8286（L4/L9/L10/L11 全过、L11 3/3、物理 overlap 0；L8 11/12 的唯一 `B_L8_0057` 是修订语音在 launch 后 1.557s 才开口（>δ1.5），旧 READ 无法净掉的真实固定窗天花板，非构建 bug）；B-dev 无 L7，未偷看 test。**attr 第一轮**（Omni audio 串行，独立空 cache）：A 7/54→5/54（0 gain / 2 loss，含 A-L4 护栏损失；L12 3/3 不变），B 5/35→7/35（2 gain 落 L8；L11 不变），合并 12/89→12/89，故 **FAIL：靶向 0 增益 + 护栏损失**；未改 prompt、未跑第二轮。在线 450/450 HTTP 200；停服后四臂 136/134/89/91 hits、全 0 miss，cache/report/178 份逐夹 JSON 逐字节同；端口关闭、GPU 清零。**v2.3 test 仍 0 次，scorer v4 未冻结。** Git 可达 refs 0 missing、HEAD+origin scoped fsck 过；generic auto-gc/full fsck 仍会报历史不可达 `3a1cb90…` 缺父 `d215c907…`，不影响当前历史，本轮未清理。

- **attr 判定 + 冻结 v4 + v2.3 test 协议（7/17 判读层；`rb_design.md` §16.6 + `rb_test_protocol.md` §九）**：镜像容器对四臂 178 份逐夹行独立重算（headline 7/5/5/7、配对集、L12 3/3↔3/3、护栏 1/17→0/17）**全部与收据逐位一致**；`A_L4_0055` 逐轨迹复核把机制钉死——baseline dec0 全链 launch + dec1 把 seat_class 正确 patch 到 hold_seat(op3)（**无规则时绑定本来是对的**），attr 臂变增量 launch + 把 seat_class patch 进 search_trains(op2)（**在规则明文禁止的方向上违反规则**）。**裁决 = attr 提示词门封盘于第一轮，第二轮额度不动用**：结构性死因三联（靶层惰性/靶维反作用/弥散副作用）不是措辞可修形态；dev 靶层 L12 n=3 在天花板（按 v2.2.1 实测 ~14% 误绑率，3/3 概率 ≈64%，非"问题消失"证据），第二轮在结果面结构性无法显示靶向增益。与 FDB 五靶"词条工程刀口零和"同构且更强。WHICH 轴正确机制类入册不建造：① 引擎级 patch 模式校验（diff 字段不在目标 op 参数表即拒绝——可机械拦 A_L4_0055 形态、拦不住合法字段错语义形态）② decider 结构化绑定槽；L12 保留为测量层（test n=36 基线误绑率 = 论文定量刻画，机制修复 = open problem）。**scorer freeze v4**：三文件新哈希 + v2.3 七项勘误链（reverse 净额/执行窗+abort/comp_cost 豁免 aborted/state 排 void/judge prompt/META 键/occurrence 时延/REVERSE 映射）+ dev23 oracle 门收据 + build 钉死 `e1a515c29b8a`；runner test 守卫复验 OK。**§九 = v2.3 test-911 协议冻结**（15 providers 各一次：`rbt23_*`；FC 三档臂 A 限定——臂 B L10/L11 事件锚在 tts_start，改 say 策略=改考题；armb_timing overlaps=0 硬门；R1–R8 判读含 H-COMP1 诚实逆风预注册〔blocking 决到静默从不早提交，预测其 L7 免费通过 ≥ TACT〕与 H-COMP2〔reverse 调用发生率，=0 则"补偿是能力缺口而非机制缺口"成立〕；H-B1 不再重冻）。**下一步 = 用户按 §九 test day；判读写 §十；随后文稿装配线（金子集录制、四基准全文核对两项并行任务不变）。**

### 既往（W3 D1–D3 + D1.5/D2 定界批，2026-07-06）

- **D3 提前交付**：三疑点逐数字核验 + 17×7 逐夹台账 `docs/w3_ledger.md`（真差分 = 赢 eco19/**hou21**/hou25、输 hou17b；travel_23 勘误已挂 w2_rerun_report；delta_hist **退役**；静默预算定律 δ>Σ静默(launch→救援EoU)，阈值 0.64/0.64/1.12/3.91；McNemar 修正 4 对 p=0.625）。神谕 06 批复：E2 闭合；**屏障裁断 = 显式设计（decision-atomic commit），grid v1 零重跑，永不重议**；prompt 批次定稿五靶；λ(t) v2 换静默钟重建。
- **D1 接线（代码交付，待手动验证）**：语义单源 **`src/tact_core.py`**（WindowLedger = 静默钟窗 + 提交屏障 + 双戳 + 三释放路径；决策应用/解析修复/prompt&snapshot v2 全部从 harness 迁入）；`src/engine_b.py` 重写为 Phase-B v1（v0 commit-后开窗语义退役；engine_cfg: phase/mode/delta/commit_barrier/stale_eou_policy/tool_sync/sv_alpha 占位）；`scripts/w2r_stream_replay.py` 降级为驱动器（`--commit-barrier on|off`、`--engine core|full`：core = 逐位契约路径，full = 帧→TactEngine 的 H6 仪器，仅 tact 模式）；backend.py `engine.phase: b` 分支接 live。规格 `docs/engine_b_spec.md`（五条款 + stale_eou_policy=apply 的 W2 逐位论证 + 感知差清单）。
- **验收工具**：`scripts/w3_barrier_probe.py`（四夹双语义 + rollback-17 on 12/17 / off **10/17** + 对 `result_w2r_tact_d100/d150` 全部确定性字段逐位对账——墙钟分量仅信息化：v1 档案 tool_wall 系 --workers 全局 RNG 打乱产物非权威，留 1.0s sanity 界；exit code 硬判；探针 provider 命名 w3p_*，不碰 w2r_* 冻结档）；`tests/test_commit_barrier.py`（纯 CPU、无模型无网络，37 项）。**跑前修正（预注册纪律）**：eco19 的救援 patch 是值中性 diff，其翻转为窗口保持 pending 的快照效应（barrier-off 下 δ≥1.0 仍过）——屏障因果权重收窄为 {hou25, fin12b}，06 预注册的 off=9/17 修正为 10/17；勘误挂 w3_ledger §4A 与 engine_b_spec §4。**已实测兑现**：机制判据全绿（on 12/17 / off 10/17 / d100 四夹全对、deferral 双戳 0.14/0.64 与台账算术吻合、80/80 缓存命中零 GPU）。W3 起 latency 增补 `completion_nominal_s`（墙钟免疫的完成锚）。
- **D1 验证收口（2026-07-06 当日完成）**：① 37/37 ✅ → ② core 探针全绿（VERDICT MET / exit 0 / on 12/17 off 10/17 / parity 21/21 逐位 + 10 条墙钟 info / deferral 双戳与台账吻合 / 80 缓存命中零 GPU）✅ → ③ S2 过（ack 层已 v1 重写 4/4；deterministic_replay 速度断言环境性挂，算力日复验）✅ → ④ full 仪器 13 偏差全归因：**ε 带 EoU 存在性效应**（间隙 0.67–0.708 三夹多 EoU 结构在流式感知下坍缩：eco19/hou25 转挂、hou17b 转过；ε 实测 0.03–0.13s）+ fin23 刀口翻转（prompt 五靶#4 证据）；serving 栈混杂（84g 临时配置）已记档 → ⑤ HumDial：三判 63.25±3.13 对 64.0 Δ=−0.75 门过、llm_timeout 0、`tact_*` 0、TTS 轮数 95/100 一致（5 夹差异疑 server 数值面非引擎）。完整记录 `docs/w3_d1_memo.md`（含 §6 补充实验）；神谕汇报稿（**缓发**，已并入 `手工文档/神谕/06_W3 执行汇报.md` 第一部分）。**D1.5 定界批已清账（7/06 晚）**：① HumDial 对称基线——W2 存档输出同日三判 61.17±1.94（7/03 单判 63.88 ⇒ **judge 供方跨日漂移 −2.7pt 实测**；新纪律：门槛比较必须同日同批对称判）；配对后 W3−W2 = **+2.08 正增益过门**（−0.75 系不对称伪差，勘销）。② server 漂移定界：10 缓存键绕缓存重调 **OPS-STABLE**（字节同 8/10，ops 同 10/10，漂移仅 say/dialogue 表面）⇒ fin23/hou17b 翻转主因 = ε 前缀效应。③ HumDial 生成栈已入档：`exp/w3/qwen3_omni_audio_84g.yaml`。④ **首响分解（裁断 B 回填，零 GPU）**：first = 0.64 hold + infer_final **恒等式零残差**；infer_final p50 0.501/p90 0.74；fallback（say 空）10/99；投机派发投影 first' p50 **0.64**（P3 比 0.786→**0.441 达标**）⇒ speculative_dispatch 维持 D6 实装。工具：`scripts/w3_server_drift_probe.py`、`scripts/w3_first_decomp.py`、`scripts/w3_d2_gap.py`。**⑤ D2 已完成（7/06 晚，H6 结案）**：full-100 `--engine full`（w3_tactfull_live，173 新决策）vs 离线 w2r_tact_full = **56 vs 56，差距 +0**；8 翻转逐例归因两两相消（ε 结构 4：eco19/eco25/hou25 挂、hou17b 过；前缀刀口决策 4：fin04/fin23/**travel_23** 过、fin22 挂）；感知敏感率 ≈8%（结构 4%+决策 4%），聚合不变。A 档双跑 run2 确定性字段 **100/100 逐位一致**（仅 2 夹 tool_wall 毫秒抖，completion_nominal_s 稳定）。延迟口径落定：pass=离线 core，延迟=W2 串行 live 档+D6 标准卡复测，full 轨管 H6/鲁棒性。E1 判据双满足（差 0 + 逐例归因）；H1/H2/H5 不再针对 pass 率启动（转真 websocket 演示链路问题）。**⑥ 零 GPU 并行批已清（7/06 晚）**：oracle 前沿三数（E4 ✓）= **固定 123.8s / oracle 20.9s / 回收上限 83.1%**（名义钟，96 可比场景；G2'(ii) 靶 = 自适应臂回收 ≥51.5s/百场景；travel_10 救援/撤销成本比 2.61×，cancel 规则零边际保费坐实 prompt 五靶#2）；hou11 契约审计闭合 = **官方 SUT 同样不喂前文（基准属性，不加 feed_history）**，该类恰 2/100（housing_11+ecommerce_12，全系统不可赢）；链式计数 = **28/100 无依赖 ≥2 调用**（A-iv：DAG claim 有据，链式修订覆盖为零→RB L7）；RB 设计文档 v1 `docs/rb_design.md`（80 规模/七层/ε 带 L3+竞态区 L4/post-EoU≥55%/屏障 held-out 消融）；04 勘误指针挂讫。**递延**：λ(t) v2（需 SenseVoice 多 cue 对齐工具）。工具：`scripts/w3_oracle_frontier.py`、`scripts/w3_hou11_census.py`。**下一步：D2（标准栈 live 全量重测 + 首响分解，ε 带发现列残差首查）+ 零 GPU 并行批（oracle 前沿三数 / hou11 契约审计与普查 / λ(t) v2 / RB 增补）。**

## 历史状态（W2 —— 重跑收口，2026-07-03）

**警示**：W2 首轮（17 代理并发，13:45–14:25）交付的实验结论**全部无效**（oracle 抄答案 / mock 数据 / 对照臂损坏），全部无效文档、伪 result 文件、mock 产物与失效脚本已**物理删除**（docs/ 只留 w1_*、fdbv3_memo、w2_rerun_report）。**有效结论只看 `docs/w2_rerun_report.md`（技术版）与 `手工文档/神谕/04_W2 执行汇报.md`（对神谕）。** 教训固化：多代理只用于写代码；实验结论必须单线程产出、逐数字对原始产物核验。

- G1'：P1 ✅（TACT@δ* exact 0.560 vs blocking 0.570；judge 0.693±0.015 vs 0.700）；P2 ⚠️（judge 轨 76.5% ✓ / 状态轨 70.6% < 85% ✗）；P3 ❌ 结构性（沙箱工具 p50 0.315s 无重叠空间=R9 兑现，双档评测预案待神谕裁断）；P4 ✅（HumDial 64.0 vs 63.88）；P5 ✅（三路缓存独立跑 100/100 工具序列逐位一致；解析末态失败 0，首试失败 1.6% 全部一次修复回收）。
- δ 扫描（rollback 17 夹，nominal infer）：δ*=1.5s，exact 0.706 **反超** sblock 0.588；eager δ=0 垫底 0.529（脏轨迹被 precision=1 处决）；**首响与 δ 解耦**（ack 占锚，平坦 1.64s），代价全在完成锚 ≈ +δ 线性。D2 直方图（VAD 段×SenseVoice token 真实对齐）：14/17 更正与意图同 VAD 段（EoU 粒度天然不暴露），跨段暴露间隙 0.42/1.12/1.16s——δ* 与间隙+决策延迟吻合，理论闭环第一圈成立。
- 全量 100（串行 live，独占）：TACT 首响 p50 1.141s / 完成 2.943s vs 流式 blocking 1.452 / 1.452。**决策延迟修正：整段工具决策 p50=0.444s**（此前 ~1.0s 是与 HumDial 管线共抢 vLLM 的污染值——延迟纪律教训第二次兑现）。首响物理地板 = 0.64 hold + 0.444 ≈ 1.08s；P3 要 ≤0.73s ⇒ 决策须 ≤0.09s ⇒ W3 增量决策/4B 头是唯一路径。
- **0.71<0.73 结案**（同 judge 重判 0.670 vs 0.690）：差距=2 场景——finance_14 同名调用顺序互换被官方按位置 pop(0) 对齐双杀（生态问题，提交时排序即修复）+ travel_16 实体逐字（Vegas/Las Vegas）。"急切调用被处决"证伪（0 例）。
- **R12 结案**：judge-pass 轨工具选择仍是二元 precision=1（严苛）；只有 evaluate_tool_calls 的 F1 渐变（宽恕，且带 turn_take_success=转写非空门）。双轨叙事按此分层。
- 状态轨判分器校准：blocking 上 TF=FT=0（≡exact，无虚增）；TACT 上 TF=1（ecommerce_15 先错后对=脏轨迹终态正确实例）。参数比较逐字沿用官方 exact_match_args。
- **judge 噪声带**：同文件三次重判 ±2pt（provider 侧非确定）。判读纪律：≤2pt judge 差不构成证据。
- ack-v0 实测（干净 provenance）：整句 TTS 0.933s → ack 0.429s，首音频提前 53.8%。
- 关键实现事实（W3 继承）：评测 harness=`scripts/w2r_stream_replay.py`（EoU=VAD 段尾+0.64s hold；异议窗音频钟、用户语音暂停倒计时、patch 重启窗；快照必须含已执行集否则跨 EoU 重复 launch；launch 幂等去重；参数 schema 矫正；salvage 解析器；**快照 op_id 必须局部编号**——全局计数器在并发下会使 prompt 漂移）。判分 `scripts/w2r_score_grid.py`；状态轨 `scripts/w2r_state_track.py`；直方图 `scripts/w2r_delta_hist.py`。
- rollback 21 中 6 场景无音频（released 只有 100 夹）；"回滚 21"物理上=15 场景/17 夹。
- **评测效率基建**（准确度回归轨；出数/确定性验证回 A 档串行）：harness `--workers 12` 全量 100 从 ~25min → 98s（thread-local VAD/HTTP、缓存加锁、nominal-infer 推进音频钟）；DeepSeek judge `FDB_LLM_WORKERS=100`（100 场景 ≈40s，run_fdb_with_deepseek.sh 已固化）；决策缓存跨 δ 网格复用（6 点 ≈1 点开销）。

**W3 入口弹药**：双档延迟评测裁断（R9）；增量决策/4B 头压首响地板（0.09s 目标）；live 差距重查（H6→H1→H2→H5→H3→H4）；状态轨 85% 缺口主体=与 blocking 共通的 ASR/实体逐字错误（非事务机制），实体规范化是否立项待裁断；patch 质量迭代（housing_17 打错字段，这次守 30 条子集纪律）；上游 PR 候选（latency_injector per-instance RNG、同名调用对齐语义）。

## 历史状态（W1 —— 已收口，2026-07-03 GPU 日）

**G0 达成**：感知冻结消灭 + 音频钟迁移 + 行为保持验证 + FDB 跑通。详见 `docs/w1_report.md`。

- [x] 全部 D0–D6 交付（引擎/工具/审计/双语/并发，提交历史 8dc0876..HEAD）
- [x] 真 LLM 等价性：分类决策 19/20 一致；legacy 自复现地板 2/3（墙钟抖动×T=0）；injected 回放保真 L1 20/20 @ 60× 实时
- [x] 冻结测量（真模型）：legacy 每 clip 停摆 0.5–0.9s vs actor 0（`docs/w1_freeze_real.json`）
- [x] HumDial 100 回归（seed 42 对齐 6/23）：TTS 轮数 94/100 一致；7 例为旧引擎丢 ASR 竞态（新引擎修复）；`llm_timeout` 0 触发。**分数级 judge 待 DEEPSEEK_API_KEY**
- [x] FDB-v3 blocking 冒烟 6/6 PASS（官方 scorer）；决策延迟基线：分类 p50 0.10s / response 0.15s / TTS 0.66s
- [x] 并发轨验证：24 会话 @ 并发 8 = 14s（mock）；SenseVoice 双语 ASR 验收（RTF 0.021, EN 带标点）

**W2 入口弹药**（蓝图 §4.2–4.3）：决策延迟基线已测；guided_json 可用性待查（vLLM 版本支持 structured output 大概率 ok，W2 Day1 确认）；PendingSet 原型现位于 `tact/transaction.py`；FDB 离线契约已验证。
