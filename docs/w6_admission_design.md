# W6 — SV 门控（记录占位；当前工作仍在 W5）

> 2026-07-16 用户裁定（第二次）：**W6 收窄为 SV 门控单项**，留档不开工；
> 原 W6 里的归属门（WHICH 轴）改为 W5 当期直接实现（`scripts/rb_run.py`
> `--attr on` + `PROMPT_RB_ATTR`，rb_design §15.2）。本文件仅保留 SV 门的
> 立项记录，供未来开工时展开为完整预注册。

## 记录

- **动机（已量化，v2.2.1 test）**：L10 参数流双率——TACT 第三方命令拒绝
  .500(A)/.154(B) vs blocking .929/.692；良性本人修订穿透 .615(A) 是要保住的
  收益。异议窗 = 修复窗 = 攻击面；SV 门 = 蓝图 §4.6 的复活。
- **机制草案**：决策级准入——触发段说话人验证失败 ⇒ 丢弃该决策全部 ops
  （say 不受影响）。两行：SV-oracle（构建 voice 标签 = 上界）+ SV-real
  （CAM++/ERes2Net 类嵌入器，enrollment = 首个用户段，τ 在 dev 冻结）。
- **结构面（已核验）**：多声音 episode 全部且只在 L10 ⇒ 门在其余夹结构性
  不触发（v2.2.1 为 26/897；v2.3 重建后重验）。
- **开工条件**：用户明示。届时按两段式预注册（结构冻结 → dev 定 τ → 单发）
  展开，判读/kill 判据在开工版写定。
